# Bytecode
__pycache__/
*.py[cod]

# Credenciales: nunca deben salir del equipo
nodes/gemini_api_key.txt
*_api_key.txt
*.key

# Copias de trabajo
*_bak.py
*_bak.js

# Datos del usuario, no del repo: lo que se guarda desde los nodos.
# prompt_projects/ lo crea Multi-Prompt al salvar un proyecto, y ahi van los
# prompts de quien lo use. / User data written by the nodes, not repo content.
prompt_lists/
prompt_projects/

# Copias antes de un rediseno. La extension evita que el cargador las vea:
# recorre *.py, asi que un .py_bak no se importa dos veces.
*.py_bak
*.js_bak

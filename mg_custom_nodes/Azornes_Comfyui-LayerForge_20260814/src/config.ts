/**
 * Backward-compatible exports for the LayerForge logging configuration.
 *
 * The canonical configuration lives in `src/log_system/config.ts`, matching
 * the Model Resolver project layout.
 */
export {
    DEFAULT_LOGGER_NAME,
    LOG_LEVEL,
    LOG_MODULE_NAME,
    USE_COLORS,
} from './log_system/config.js';

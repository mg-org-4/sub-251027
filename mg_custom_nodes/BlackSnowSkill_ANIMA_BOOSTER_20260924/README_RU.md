# ANIMA_BOOSTER (BSS)

**ANIMA_BOOSTER (BSS)** — это высокотехнологичный пакет оптимизации для модели **Anima DiT 2B** в ComfyUI. Разработан для обеспечения максимальной производительности, снижения потребления видеопамяти (VRAM) и ускорения генерации на видеокартах NVIDIA.

> [!IMPORTANT]
> **Автор и разработчик:** **blacksnowskill (BSS)**
> **© 2026 blacksnowskill (BSS). Все права защищены.**
> Данный проект защищен авторским правом. Любое несанкционированное копирование, модификация без указания авторства или выдача данного кода за свой продукт строго запрещены.

Этот пакет позволяет получить **суммарное ускорение от 3.5× до 5.0×** по сравнению со стандартным воркфлоу Anima в ComfyUI без потери визуального качества.

> [!TIP]
> **Максимальное качество и восстановление деталей:**  
> Хотя экстремальное ускорение может иногда приводить к незначительной потере микродеталей, у нас есть идеальное решение. Мы настоятельно рекомендуем использовать **ANIMA_BOOSTER** в паре с нашим сэмплером **[FLSampler (BSS)](https://github.com/BlackSnowSkill/ComfyUI-BSS_FLSampler)**. FLS идеально восстанавливает любые потерянные детали без снижения скорости генерации, создавая даже более четкие и проработанные текстуры, чем оригинальная неоптимизированная модель!

---

## ⚡ Ключевые возможности

* **Интегрированная JIT-компиляция (`torch.compile`):** Безопасная компиляция блоков DiT в один клик непосредственно в загрузчике. Работает на стабильном бэкенде `inductor` без использования CUDA Graphs, обеспечивая стабильность и прирост скорости на **20–40%**.
* **SageAttention:** Встроенная поддержка сверхбыстрого 8-битного внимания для моделей DiT, значительно ускоряющая вычисления при сниженном потреблении памяти.
* **Адаптивный TeaCache:** Интеллектуальное кэширование скрытых состояний. Динамически регулирует шаг кэширования: снижает его на ранних шагах для сохранения геометрии и повышает на поздних шагах для максимального ускорения.
* **BSS Premium UI:** Интегрированная темная высококонтрастная тема оформления для нод пакета. Включает полностью переработанное управление слайдерами по всей ширине, адаптивное скрытие полей для неактивных пресетов и полную блокировку назойливых всплывающих подсказок (tooltips) для комфортной работы.

---

## ⚡ Быстрый обзор производительности

| Конфигурация | Среднее ускорение | Описание |
|---|---|---|
| Только fp16 | **1.4×** | Базовая оптимизация точности |
| fp16 + **SageAttention** | **1.8–2.5×** | Сверхбыстрое 8-битное внимание для DiT |
| fp16 + **Adaptive TeaCache** | **1.5–2.0×** | Интеллектуальное кэширование шагов |
| **fp16 + SageAttn + TeaCache** | 🚀 **2.5–3.5×** | Идеальный баланс скорости и качества |
| **+ встроенный torch.compile** | 💎 **3.5–5.0×** | Максимальный прирост (после фазы прогрева в 2–3 шага) |

---

## 📥 Установка

### Способ 1: Через ComfyUI Manager (Рекомендуется)
1. Откройте ComfyUI и нажмите кнопку **Manager**.
2. Нажмите **Install via Git URL**.
3. Вставьте ссылку на этот репозиторий: `https://github.com/BlackSnowSkill/ANIMA_BOOSTER`
4. Нажмите **Install**, дождитесь завершения установки и перезагрузите ComfyUI.

### Способ 2: Ручная установка (Git Clone)
1. Откройте терминал и перейдите в папку кастомных нод ComfyUI:
   ```bash
   cd ComfyUI/custom_nodes
   ```
2. Склонируйте репозиторий:
   ```bash
   git clone https://github.com/BlackSnowSkill/ANIMA_BOOSTER.git
   ```
3. Перезапустите ComfyUI.

---

## 🧩 Список нод пакета `BSS/AnimaBooster`

Все ноды зарегистрированы в категории `BSS/AnimaBooster`:

1. 📥 **Anima Booster Loader (BSS)** (класс `AnimaBoosterLoader`)
   - Загружает модель Anima DiT в оптимизированном формате fp16.
   - **SageAttention**: Автоматически применяет ускоренное 8-битное внимание при наличии в системе. При отсутствии — бесшовно переключается на встроенный PyTorch SDPA.
   - **Torch Compile**: Встроенный тумблер для безопасной JIT-компиляции блоков трансформера.
2. 🎛️ **Anima TeaCache (BSS)** (класс `AnimaTeaCache`)
   - Реализует адаптивное кэширование скрытых состояний на основе шагов очистки шума (TeaCache).
   - **Селектор версий (teacache_version)**: Позволяет выбирать между двумя режимами:
     * `v1 (Legacy Fast)` (По умолчанию): Восстанавливает полюбившееся пользователям агрессивное кэширование с фиксированным нормализатором шагов. Дает мгновенное ускорение до 2.0× "из коробки" на SDE-семплерах (таких как `er_sde`, `sde gpu`), хотя и может вносить незначительные артефакты на Euler A.
     * `v2 (Standard Precise)`: Математически точная, динамическая нормализация таймстепов под любые типы планировщиков и семплеров. Полностью защищает ранние структурные шаги и гарантирует идеальное качество.
3. 🖼️ **Anima Latent Image (BSS)** (класс `AnimaLatentImage`)
   - Утилита для генерации пустых латентов с автоматическим выравниванием размеров под патчи Anima DiT (2x2), предотвращающая ошибки несоответствия размерностей тензоров. Предоставляет готовые пресеты соотношений сторон.

---

## 📈 Адаптивный порог TeaCache (Adaptive TeaCache)

В отличие от стандартных реализаций TeaCache с фиксированным порогом (threshold), версия от **BSS** использует **динамический адаптивный порог**, меняющийся в процессе денойзинга:
- **На ранних шагах** (высокий уровень шума, формируется структура изображения) порог снижается автоматически для обеспечения максимальной точности прорисовки геометрии.
- **На поздних шагах** (детали стабилизировались, идет микро-текстурирование) порог повышается, позволяя пропускать до 80% вычислений блоков без потери качества.

> [!TIP]
> **Выбор режима нормализации таймстепов (в версии v1.3.0):**
> * Если вам нужна **бескомпромиссная экстремальная скорость** "из коробки" на SDE-семплерах, и вы готовы к экспериментам — используйте `v1 (Legacy Fast)`.
> * Если вы используете Euler A, цените **максимальную точность геометрии** или хотите скрупулезно настроить качество с помощью параметров `early_steps_factor` и `late_steps_factor` — выбирайте `v2 (Standard Precise)`.

---

## 📋 Рекомендуемый воркфлоу (Workflow)

Для достижения максимального эффекта подключайте ноды в следующем порядке:

```
[ Anima Booster Loader (BSS) ] ── (Включите sage_attention: auto и torch_compile: True)
             ↓ (MODEL)
[ Anima TeaCache (BSS) ] (Рекомендуется threshold: 0.15, adaptive: ON)
             ↓ (MODEL)
        [ KSampler ]
```

---

## 🛠️ Установка и зависимости на Windows

Все ресурсоемкие библиотеки оптимизации (**SageAttention** и JIT-компилятор **Triton**) являются **полностью опциональными**. Пакет спроектирован по принципу *Graceful Degradation*: если библиотеки или компиляторы не установлены, ноды автоматически отключают оптимизации, бесшовно переходя на стандартные механизмы PyTorch, что гарантирует работу без сбоев и "крашей" ComfyUI.

> [!IMPORTANT]
> **Triton необходим как для работы SageAttention, так и для JIT-компиляции (`torch.compile`)**!
> Если вы хотите включить `torch_compile` (дающую до +40% к скорости генерации) или использовать SageAttention на Windows, вам **необходимо** установить `triton-windows`. Без него JIT-компиляция будет автоматически и безопасно отключена с выводом предупреждения в консоль, чтобы избежать падений.

### 📦 Установка Triton и SageAttention на Windows (для Portable Build)

Портативные сборки ComfyUI (использующие изолированное окружение `python_embeded` на Python 3.12 или 3.13) не имеют установленных инструментов компиляции C++ (MSVC / Build Tools). Из-за этого стандартная команда `pip install sageattention` завершается ошибкой.

Для успешной установки используйте предкомпилированные бинарные пакеты (`.whl` wheel):

1. Откройте командную строку (CMD или PowerShell) в вашей корневой папке ComfyUI.
2. Установите Triton для Windows:
   ```bash
   .\python_embeded\python.exe -m pip install triton-windows
   ```
3. Скачайте предкомпилированный файл `.whl` для SageAttention, соответствующий вашей версии Python (например, `cp312` или `cp313`) и CUDA (например, `cu124` / `cu128`):
   - Предкомпилированные сборки можно найти в релизах репозитория: **[sdbds/SageAttention-for-windows](https://github.com/sdbds/SageAttention-for-windows)**.
   - Также официальные колеса публикуются в проекте: **[wildminder/AI-windows-whl](https://github.com/wildminder/AI-windows-whl)**.
4. Установите скачанный файл в портативное окружение:
   ```bash
   .\python_embeded\python.exe -m pip install <путь_к_скачанному_файлу.whl>
   ```

---

## 🐧 Установка и зависимости на Linux (Ubuntu)

В операционных системах Linux/Ubuntu процесс установки значительно проще, чем на Windows, так как компиляторы и сборочные утилиты встроены в систему по умолчанию.

### 📦 Предварительные требования

Убедитесь, что в вашей системе установлен CUDA Toolkit и компилятор `nvcc` доступен в окружении (проверить можно командой `nvcc --version`).

### 📦 Установка SageAttention 2.x

Рекомендуется использовать SageAttention версии 2.x для современных видеокарт (архитектуры Ampere, Lovelace, Blackwell, например, серии RTX 30xx, 40xx, 50xx).

1. Активируйте виртуальное окружение ComfyUI:
   ```bash
   source /путь_к_ComfyUI/venv/bin/activate
   ```
2. Установите SageAttention напрямую из PyPI с флагом `--no-build-isolation`, чтобы исключить конфликты зависимостей с PyTorch:
   ```bash
   pip install sageattention --no-build-isolation
   ```
   *Если стандартная установка не удалась, скомпилируйте из исходного кода:*
   ```bash
   git clone https://github.com/thu-ml/SageAttention.git
   cd SageAttention
   pip install "setuptools<=75.8.2"
   pip install --no-build-isolation -e .
   ```

---

## 🔍 Решение проблем: Ошибки Triton JIT и `PassManager::run failed`

Если вы включили параметр `torch_compile` в ноде загрузчика и получили сбой в KSampler с ошибкой `RuntimeError: PassManager::run failed` (возникающей при сборке ядра `_attn_fwd` компилятором Triton, особенно часто на Ubuntu 24.04 с Python 3.12) — это известная проблема несовместимости Triton с системным компилятором/библиотеками LLVM.

Чтобы решить или обойти эту проблему, попробуйте следующие варианты:

### 1. Отключение `torch_compile` (Рекомендуется)
Самый простой и надежный способ — установить параметр **`torch_compile` в значение `False`** в нодах загрузчиков **Anima Booster Loader (BSS)** или **Anima Checkpoint Loader (BSS)**.
* *Почему это помогает:* Ноды пакета отлично оптимизированы. Даже с выключенной компиляцией вы получите колоссальный прирост скорости (**в 2.5–3.5 раза**) за счет работы **SageAttention** и **TeaCache**, при этом полностью исключите нестабильный JIT-компилятор Triton.

### 2. Отключение оптимизаций Triton JIT
Вы можете заставить Triton пропустить проблемный шаг оптимизации компиляции. Для этого запустите ComfyUI с переменной окружения:
```bash
export TRITON_JIT_DISABLE_OPT=1
python main.py
```

### 3. Очистка кэша компилятора Triton
Иногда кэшированные ядра компилируются некорректно. Удалите папки кэша:
```bash
rm -rf ~/.triton/cache
rm -rf ~/.cache/triton
rm -rf /tmp/torchinductor_*
```

### 4. Принудительное обновление Triton
Убедитесь, что установлена корректная версия Triton, совместимая с вашей версией PyTorch:
```bash
pip install --upgrade --force-reinstall triton
```



## 🧑‍💻 Технические детали реализации для разработчиков

- **Основа модели**: Anima DiT базируется на архитектуре `MiniTrainDIT` с адаптером `LLMAdapter`.
- **Точка интеграции SageAttention**: Патч накладывается через стандартный словарь параметров `transformer_options["optimized_attention_override"]`.
- **Точка интеграции Torch Compile**: Вызов встроенной функции `set_torch_compile_wrapper` из `comfy_api.torch_helpers` на уровне индивидуальных блоков трансформера (что обеспечивает LoRA-совместимость и снижает оверхед компиляции).
- **Изолированность**: Все изменения структуры графа и весов производятся исключительно на клонированной копии модели (`model.clone()`). Это исключает конфликты и оставляет оригинальную модель в кэше ComfyUI нетронутой.

---

## ☕ Поддержка и разработка

Если вам нравятся мои проекты и вы хотите поддержать создание новых оптимизаций, нод и моделей, вы можете поддержать меня здесь:
- **Boosty**: [Поддержка и эксклюзивные модели](https://boosty.to/blacksnowskill)

---

## 📄 Лицензия и условия использования

© 2026 blacksnowskill (BSS). Все права защищены.

Программное обеспечение предоставляется как экспериментальная сборка. Буду рад вашим отзывам и предложениям.
**Примечание:** Данный проект защищен авторским правом. Любое несанкционированное копирование, распространение, слияние с другими проектами или размещение на сторонних репозиториях/сайтах без явного письменного разрешения автора строго запрещено.

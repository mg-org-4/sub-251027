# Example Gallery

Use this directory to store curated renders that demonstrate different loader/generator combos and prompt rewriting modes.

Recommended naming: `workflow-name_resolution_seed.png` with a matching `.md` snippet describing prompt, steps, sampler, and any notable settings.

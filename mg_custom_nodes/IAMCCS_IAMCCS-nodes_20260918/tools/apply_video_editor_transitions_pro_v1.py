from __future__ import annotations

from pathlib import Path
import py_compile
import shutil

MARKER = "IAMCCS_TRANSITIONS_PRO_V1"
ROOT = Path(__file__).resolve().parent
if not (ROOT / "web").is_dir() and (ROOT.parent / "web").is_dir():
    ROOT = ROOT.parent

UI = ROOT / "web" / "iamccs_shotboard_video_editor_v1_ui.js"
BACKEND = ROOT / "cine_multigeneration" / "__init__.py"

UI_BACKUP = UI.with_name(UI.name + ".pre_transitions_pro_v1.bak")
BACKEND_BACKUP = BACKEND.with_name(BACKEND.name + ".pre_transitions_pro_v1.bak")

UI_INSERT = '\n  // IAMCCS_TRANSITIONS_PRO_V1\n  function transitionEnsureStyle() {\n    if (document.getElementById("iamccs-transitions-pro-v1-style")) return;\n    const style = document.createElement("style");\n    style.id = "iamccs-transitions-pro-v1-style";\n    style.textContent = `\n      .iamccs-transitions-pro-panel {\n        position:fixed; z-index:2147483600; width:340px; max-width:calc(100vw - 16px);\n        padding:10px; border:1px solid #d7b667; background:linear-gradient(180deg,#263033,#0d1315);\n        color:#edf4ef; box-shadow:0 14px 34px rgba(0,0,0,.62),0 0 0 1px rgba(255,226,168,.10) inset;\n        font:11px/1.35 Inter,system-ui,sans-serif;\n      }\n      .iamccs-transitions-pro-panel .head { display:flex; align-items:center; gap:8px; margin-bottom:9px; }\n      .iamccs-transitions-pro-panel .head strong { color:#ffe4a2; font-size:12px; font-weight:900; }\n      .iamccs-transitions-pro-panel .head .clip { margin-left:auto; max-width:180px; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; color:#9fc5c8; }\n      .iamccs-transitions-pro-panel .grid { display:grid; grid-template-columns:94px 1fr; gap:7px 8px; align-items:center; }\n      .iamccs-transitions-pro-panel label { color:#b8c9c7; font-weight:800; }\n      .iamccs-transitions-pro-panel select,\n      .iamccs-transitions-pro-panel input {\n        width:100%; height:27px; border:1px solid #58696d; background:#111b1d; color:#eef7f2; padding:0 7px;\n        font:11px Consolas,monospace;\n      }\n      .iamccs-transitions-pro-panel .hint { margin-top:8px; color:#91a7aa; font-size:10px; min-height:28px; }\n      .iamccs-transitions-pro-panel .actions { display:flex; justify-content:flex-end; gap:6px; margin-top:10px; }\n      .iamccs-transitions-pro-panel button {\n        min-width:74px; height:27px; border:1px solid #687b7e; background:#182326; color:#e7f1e8;\n        font-size:10px; font-weight:900; cursor:pointer;\n      }\n      .iamccs-transitions-pro-panel button.apply { border-color:#e0a85d; background:linear-gradient(180deg,#70451f,#3b2415); color:#ffe8bd; }\n      .iamccs-transitions-pro-panel button.clear { border-color:#a96b6b; color:#ffc8c8; }\n    `;\n    document.head.appendChild(style);\n  }\n\n  function transitionSelectedVideoClip() {\n    const selected = (manifest.clips || []).find((clip) => clip?.id === selectedClipId && clip?.type === "video");\n    return selected || clipAtTime("video");\n  }\n\n  function transitionSameLaneVideos(clip) {\n    if (!clip) return [];\n    const lane = String(trackIdForClip(clip) || clip.trackId || "");\n    return (manifest.clips || [])\n      .filter((item) => item?.type === "video" && String(trackIdForClip(item) || item.trackId || "") === lane)\n      .sort((a, b) => Number(a.startTime || 0) - Number(b.startTime || 0));\n  }\n\n  function transitionLinkedAudio(videoClip) {\n    if (!videoClip) return [];\n    const ids = new Set(Array.isArray(videoClip.linkedClipIds) ? videoClip.linkedClipIds.map(String) : []);\n    const take = Number(videoClip.takeIndex || 0);\n    const slot = Number(videoClip.clipIndex || 0);\n    return (manifest.clips || []).filter((clip) => {\n      if (!clip || clip.type !== "audio" || isMasterClip(clip)) return false;\n      if (ids.has(String(clip.id || ""))) return true;\n      if (take > 0 && Number(clip.takeIndex || 0) !== take) return false;\n      if (slot > 0 && Number(clip.clipIndex || 0) !== slot) return false;\n      return take > 0 || slot > 0;\n    });\n  }\n\n  function transitionSuite(clip) {\n    clip.suite_edit = clip.suite_edit && typeof clip.suite_edit === "object" ? clip.suite_edit : {};\n    clip.suite_edit.transitions = clip.suite_edit.transitions && typeof clip.suite_edit.transitions === "object"\n      ? clip.suite_edit.transitions\n      : {};\n    return clip.suite_edit.transitions;\n  }\n\n  function transitionPayload(type, frames, fpsValue, audioMode, microBlend) {\n    const frameCount = Math.max(1, Math.min(240, Math.round(Number(frames) || 1)));\n    const fpsSafe = Math.max(1, Number(fpsValue || manifest.fps || 24));\n    return {\n      schema: "iamccs.editor.transition.v2",\n      type: String(type || "cut"),\n      duration_frames: frameCount,\n      duration: frameCount / fpsSafe,\n      alignment: "edit_point",\n      video_curve: "half_cosine",\n      audio_curve: String(audioMode || "equal_power"),\n      audio_mode: String(audioMode || "equal_power"),\n      owner: String(type || "") === "continuity" ? "incoming" : "blend",\n      micro_blend_frames: Math.max(0, Math.min(frameCount, Math.round(Number(microBlend) || 0))),\n      created_by: "IAMCCS_TRANSITIONS_PRO_V1",\n    };\n  }\n\n  function transitionShiftLinkedSequence(incoming, deltaSeconds) {\n    if (!incoming || !deltaSeconds) return;\n    const laneVideos = transitionSameLaneVideos(incoming);\n    const boundary = Number(incoming.startTime || 0);\n    const affectedVideos = laneVideos.filter((clip) => Number(clip.startTime || 0) >= boundary - 1e-6);\n    const ids = new Set();\n    for (const clip of affectedVideos) {\n      clip.startTime = Math.max(0, Number(clip.startTime || 0) + deltaSeconds);\n      ids.add(String(clip.id || ""));\n      for (const audio of transitionLinkedAudio(clip)) ids.add(String(audio.id || ""));\n    }\n    for (const clip of manifest.clips || []) {\n      if (!clip || clip.type !== "audio" || isMasterClip(clip)) continue;\n      if (ids.has(String(clip.id || ""))) {\n        const videoMatch = affectedVideos.some((video) => {\n          const linked = new Set(Array.isArray(video.linkedClipIds) ? video.linkedClipIds.map(String) : []);\n          return linked.has(String(clip.id || "")) ||\n            (Number(video.takeIndex || 0) > 0 && Number(clip.takeIndex || 0) === Number(video.takeIndex || 0) &&\n             Number(video.clipIndex || 0) === Number(clip.clipIndex || 0));\n        });\n        if (videoMatch) clip.startTime = Math.max(0, Number(clip.startTime || 0) + deltaSeconds);\n      }\n    }\n  }\n\n  function transitionRestoreIncomingShift(incoming) {\n    if (!incoming) return 0;\n    const current = transitionSuite(incoming).in;\n    const shift = Math.max(0, Number(current?.timeline_shift_seconds || 0));\n    if (!(shift > 0)) return 0;\n    transitionShiftLinkedSequence(incoming, shift);\n    return shift;\n  }\n\n  function transitionMirrorToAudio(videoClip, side, payload) {\n    for (const audio of transitionLinkedAudio(videoClip)) {\n      const transitions = transitionSuite(audio);\n      if (payload) transitions[side] = JSON.parse(JSON.stringify(payload));\n      else delete transitions[side];\n    }\n  }\n\n  function transitionPairFor(selected, side) {\n    if (!selected) return { outgoing: null, incoming: null };\n    const lane = transitionSameLaneVideos(selected);\n    const index = lane.findIndex((clip) => clip.id === selected.id);\n    if (index < 0) return { outgoing: null, incoming: null };\n    if (side === "out") return { outgoing: selected, incoming: lane[index + 1] || null };\n    return { outgoing: lane[index - 1] || null, incoming: selected };\n  }\n\n  function transitionRefreshDuration() {\n    const end = Math.max(0, ...(manifest.clips || [])\n      .filter((clip) => clip && !isMasterClip(clip))\n      .map((clip) => Number(clip.startTime || 0) + Number(clip.duration || 0)));\n    manifest.duration_seconds = Math.max(end, 0);\n  }\n\n  function transitionClearPair(outgoing, incoming) {\n    if (!incoming) return false;\n    transitionRestoreIncomingShift(incoming);\n    const inTransitions = transitionSuite(incoming);\n    delete inTransitions.in;\n    transitionMirrorToAudio(incoming, "in", null);\n    if (outgoing) {\n      const outTransitions = transitionSuite(outgoing);\n      delete outTransitions.out;\n      transitionMirrorToAudio(outgoing, "out", null);\n    }\n    transitionRefreshDuration();\n    persist();\n    renderTimeline();\n    return true;\n  }\n\n  function transitionApplyPair(outgoing, incoming, type, frames, audioMode, microBlend) {\n    if (!incoming) throw new Error("No incoming video clip exists on this lane.");\n    const fpsValue = Math.max(1, Number(manifest.fps || 24));\n    transitionRestoreIncomingShift(incoming);\n    const payload = transitionPayload(type, frames, fpsValue, audioMode, microBlend);\n    const needsOverlap = payload.type === "dissolve" || payload.type === "continuity";\n    if (needsOverlap) {\n      if (!outgoing) throw new Error("This transition needs a previous clip on the same video lane.");\n      const seconds = payload.duration;\n      payload.timeline_shift_seconds = seconds;\n      payload.timeline_shift_policy = "consume_timeline_overlap";\n      transitionShiftLinkedSequence(incoming, -seconds);\n    } else {\n      payload.timeline_shift_seconds = 0;\n      payload.timeline_shift_policy = "edit_point_no_overlap";\n    }\n\n    const inTransitions = transitionSuite(incoming);\n    inTransitions.in = JSON.parse(JSON.stringify(payload));\n    transitionMirrorToAudio(incoming, "in", payload);\n\n    if (outgoing) {\n      const outPayload = { ...payload, owner: payload.type === "continuity" ? "outgoing_trimmed_by_incoming" : "blend" };\n      const outTransitions = transitionSuite(outgoing);\n      outTransitions.out = JSON.parse(JSON.stringify(outPayload));\n      transitionMirrorToAudio(outgoing, "out", outPayload);\n    }\n\n    transitionRefreshDuration();\n    persist();\n    renderTimeline();\n    return payload;\n  }\n\n  function createTransitionsProButton() {\n    const button = makeButton("TRANS", (trigger) => {\n      transitionEnsureStyle();\n      document.querySelectorAll(".iamccs-transitions-pro-panel").forEach((item) => item.remove());\n      const selected = transitionSelectedVideoClip();\n      if (!selected) {\n        if (status) status.textContent = "TRANSITIONS: select a video clip first.";\n        return;\n      }\n\n      const panel = document.createElement("div");\n      panel.className = "iamccs-transitions-pro-panel";\n      const currentIn = transitionSuite(selected).in || {};\n      const defaultFrames = Math.max(1, Math.round(Number(currentIn.duration_frames || 12)));\n      const currentType = String(currentIn.type || "cut");\n      const currentAudio = String(currentIn.audio_mode || currentIn.audio_curve || "equal_power");\n      const currentMicro = Math.max(0, Math.round(Number(currentIn.micro_blend_frames || 6)));\n      panel.innerHTML = `\n        <div class="head"><strong>TRANSITIONS PRO</strong><span class="clip">${selected.timelineId || selected.id || "clip"}</span></div>\n        <div class="grid">\n          <label>Apply to</label>\n          <select data-field="side"><option value="in">IN of selected</option><option value="out">OUT of selected</option></select>\n          <label>Transition</label>\n          <select data-field="type">\n            <option value="cut">Cut</option>\n            <option value="dissolve">Cross Dissolve</option>\n            <option value="dip_black">Dip to Black</option>\n            <option value="dip_white">Dip to White</option>\n            <option value="continuity">Continuity Join</option>\n            <option value="motion_blend" disabled>Motion Blend / Optical Flow (reserved)</option>\n          </select>\n          <label>Duration</label>\n          <input data-field="frames" type="number" min="1" max="240" step="1" value="${defaultFrames}">\n          <label>Audio</label>\n          <select data-field="audio">\n            <option value="equal_power">Equal Power</option>\n            <option value="follow_video">Follow Video Curve</option>\n            <option value="hard_cut">Hard Cut / Incoming Owns</option>\n            <option value="soft_av">Soft AV</option>\n          </select>\n          <label>Micro blend</label>\n          <input data-field="micro" type="number" min="0" max="48" step="1" value="${currentMicro}">\n        </div>\n        <div class="hint"></div>\n        <div class="actions">\n          <button type="button" class="clear">Clear</button>\n          <button type="button" class="cancel">Cancel</button>\n          <button type="button" class="apply">Apply</button>\n        </div>\n      `;\n      document.body.appendChild(panel);\n\n      const side = panel.querySelector(\'[data-field="side"]\');\n      const type = panel.querySelector(\'[data-field="type"]\');\n      const frames = panel.querySelector(\'[data-field="frames"]\');\n      const audio = panel.querySelector(\'[data-field="audio"]\');\n      const micro = panel.querySelector(\'[data-field="micro"]\');\n      const hint = panel.querySelector(".hint");\n      type.value = ["cut","dissolve","dip_black","dip_white","continuity"].includes(currentType) ? currentType : "cut";\n      audio.value = ["equal_power","follow_video","hard_cut","soft_av"].includes(currentAudio) ? currentAudio : "equal_power";\n\n      const updateHint = () => {\n        const value = String(type.value || "cut");\n        if (value === "continuity") {\n          hint.textContent = "Incoming clip owns the overlap. Micro blend only repairs the first seam frames; ideal for generated chunk handles.";\n          if (Number(frames.value || 0) === 12) frames.value = "22";\n        } else if (value === "dissolve") {\n          hint.textContent = "Creates a real timeline overlap and renders a half-cosine video blend with equal-power audio.";\n        } else if (value === "dip_black" || value === "dip_white") {\n          hint.textContent = "No timeline overlap: outgoing fades to the dip colour, incoming fades out of it around the edit point.";\n        } else {\n          hint.textContent = "Frame-accurate hard cut. Existing transition metadata on this edit is replaced.";\n        }\n        micro.disabled = value !== "continuity";\n      };\n      type.addEventListener("change", updateHint);\n      updateHint();\n\n      const close = () => panel.remove();\n      panel.querySelector(".cancel").onclick = close;\n      panel.querySelector(".clear").onclick = () => {\n        try {\n          const pair = transitionPairFor(selected, String(side.value || "in"));\n          transitionClearPair(pair.outgoing, pair.incoming);\n          if (status) status.textContent = "Transition cleared.";\n          close();\n        } catch (error) {\n          if (status) status.textContent = `Transition clear failed: ${error?.message || error}`;\n        }\n      };\n      panel.querySelector(".apply").onclick = () => {\n        try {\n          const pair = transitionPairFor(selected, String(side.value || "in"));\n          const payload = transitionApplyPair(\n            pair.outgoing,\n            pair.incoming,\n            String(type.value || "cut"),\n            Number(frames.value || 1),\n            String(audio.value || "equal_power"),\n            Number(micro.value || 0),\n          );\n          if (status) status.textContent =\n            `Transition ${payload.type} applied (${payload.duration_frames}f, audio ${payload.audio_mode}).`;\n          close();\n        } catch (error) {\n          if (status) status.textContent = `Transition failed: ${error?.message || error}`;\n        }\n      };\n\n      const rect = trigger?.getBoundingClientRect?.();\n      const place = () => {\n        if (!panel.isConnected) return;\n        const width = panel.offsetWidth || 340;\n        const left = Math.max(8, Math.min(Number(rect?.left || 8), window.innerWidth - width - 8));\n        const top = Math.max(8, Math.min(Number(rect?.bottom || 8) + 7, window.innerHeight - panel.offsetHeight - 8));\n        panel.style.left = `${left}px`;\n        panel.style.top = `${top}px`;\n      };\n      place();\n      requestAnimationFrame(place);\n    }, "gold");\n    button.title = "Professional transition controls for the selected edit. Opens a floating box without changing the editor layout.";\n    return button;\n  }\n'
BACKEND_BLOCK = 'def _iamccs_transition_frame_count(transition: Dict[str, Any], fps: float, fallback: int = 1) -> int:\n    """IAMCCS_TRANSITIONS_PRO_V1: resolve one transition duration to frames."""\n    if not isinstance(transition, dict):\n        return max(1, int(fallback))\n    explicit = _safe_int(transition.get("duration_frames"), 0)\n    if explicit > 0:\n        return max(1, explicit)\n    seconds = max(0.0, _safe_float(transition.get("duration"), 0.0))\n    if seconds > 0:\n        return max(1, int(round(seconds * max(1.0, fps))))\n    return max(1, int(fallback))\n\n\ndef _iamccs_transition_curve(count: int, device: Any, dtype: Any, mode: str = "half_cosine") -> torch.Tensor:\n    count = max(1, int(count))\n    if count == 1:\n        return torch.ones((1, 1, 1, 1), device=device, dtype=dtype)\n    t = torch.linspace(0.0, 1.0, count, device=device, dtype=torch.float32)\n    mode = str(mode or "half_cosine").strip().lower()\n    if mode in {"linear", "lin"}:\n        alpha = t\n    else:\n        alpha = 0.5 - 0.5 * torch.cos(t * math.pi)\n    return alpha.view(-1, 1, 1, 1).to(dtype=dtype)\n\n\ndef _iamccs_audio_curve(count: int, device: Any, dtype: Any, incoming: bool, mode: str) -> torch.Tensor:\n    count = max(1, int(count))\n    if count == 1:\n        return torch.ones((1,), device=device, dtype=dtype)\n    t = torch.linspace(0.0, 1.0, count, device=device, dtype=torch.float32)\n    mode = str(mode or "equal_power").strip().lower()\n    if mode in {"equal_power", "soft_av"}:\n        gain = torch.sin(t * (math.pi / 2.0)) if incoming else torch.cos(t * (math.pi / 2.0))\n    elif mode in {"follow_video", "half_cosine"}:\n        alpha = 0.5 - 0.5 * torch.cos(t * math.pi)\n        gain = alpha if incoming else (1.0 - alpha)\n    else:\n        gain = torch.ones_like(t)\n    return gain.to(device=device, dtype=dtype)\n\n\ndef _assemble_editor_timeline_frames(entries: List[Tuple[Dict[str, Any], Any]], fps: float) -> Tuple[torch.Tensor, int]:\n    """Place edited clips at manifest time and render IAMCCS Transitions Pro."""\n    canvas = None\n    cross_dissolves = 0\n    for clip, comp in entries:\n        images = comp.images\n        if not torch.is_tensor(images) or images.ndim != 4 or int(images.shape[0]) <= 0:\n            continue\n\n        start = max(0, int(round(_safe_float(clip.get("startTime"), 0.0) * max(1.0, fps))))\n        end = start + int(images.shape[0])\n        incoming = _editor_transition(clip, "in")\n        transition_type = str(incoming.get("type") or "cut").strip().lower()\n        requested_frames = _iamccs_transition_frame_count(incoming, fps, 1)\n\n        if canvas is None:\n            previous_length = 0\n            canvas = torch.zeros((end, *images.shape[1:]), dtype=images.dtype, device=images.device)\n        else:\n            previous_length = int(canvas.shape[0])\n            if tuple(images.shape[1:]) != tuple(canvas.shape[1:]):\n                raise ValueError("IAMCCS ShotboardVideoEditorRenderV1: all parked clips must share width/height for V1 assembly.")\n            images = images.to(device=canvas.device, dtype=canvas.dtype)\n            if end > int(canvas.shape[0]):\n                canvas = torch.cat((\n                    canvas,\n                    torch.zeros(\n                        (end - int(canvas.shape[0]), *canvas.shape[1:]),\n                        dtype=canvas.dtype,\n                        device=canvas.device,\n                    ),\n                ), dim=0)\n\n        occupied_end = min(previous_length, start + int(images.shape[0]))\n        overlap = max(0, min(int(images.shape[0]), occupied_end - start))\n\n        if transition_type in {"dip_black", "dip_white"} and canvas is not None:\n            dip_value = 1.0 if transition_type == "dip_white" else 0.0\n            total = max(2, requested_frames)\n            out_count = max(1, total // 2)\n            in_count = max(1, total - out_count)\n\n            actual_out = min(out_count, max(0, min(start, previous_length)))\n            if actual_out > 0:\n                alpha_out = _iamccs_transition_curve(\n                    actual_out, canvas.device, canvas.dtype, incoming.get("video_curve", "half_cosine")\n                )\n                dip = torch.full_like(canvas[start - actual_out:start], float(dip_value))\n                canvas[start - actual_out:start] = (\n                    canvas[start - actual_out:start] * (1.0 - alpha_out)\n                    + dip * alpha_out\n                )\n\n            actual_in = min(in_count, int(images.shape[0]))\n            if actual_in > 0:\n                alpha_in = _iamccs_transition_curve(\n                    actual_in, images.device, images.dtype, incoming.get("video_curve", "half_cosine")\n                )\n                dip = torch.full_like(images[:actual_in], float(dip_value))\n                images = images.clone()\n                images[:actual_in] = dip * (1.0 - alpha_in) + images[:actual_in] * alpha_in\n\n        if transition_type == "dissolve" and overlap > 0:\n            blend_count = min(overlap, requested_frames)\n            if blend_count > 0:\n                blend_start = start\n                alpha = _iamccs_transition_curve(\n                    blend_count, canvas.device, canvas.dtype, incoming.get("video_curve", "half_cosine")\n                )\n                canvas[blend_start:blend_start + blend_count] = (\n                    canvas[blend_start:blend_start + blend_count] * (1.0 - alpha)\n                    + images[:blend_count] * alpha\n                )\n                if overlap > blend_count:\n                    canvas[blend_start + blend_count:blend_start + overlap] = images[blend_count:overlap]\n                cross_dissolves += 1\n        elif transition_type == "continuity" and overlap > 0:\n            micro = max(0, _safe_int(incoming.get("micro_blend_frames"), 0))\n            micro = min(overlap, micro)\n            if micro > 0:\n                alpha = _iamccs_transition_curve(\n                    micro, canvas.device, canvas.dtype, incoming.get("video_curve", "half_cosine")\n                )\n                canvas[start:start + micro] = (\n                    canvas[start:start + micro] * (1.0 - alpha)\n                    + images[:micro] * alpha\n                )\n            if overlap > micro:\n                canvas[start + micro:start + overlap] = images[micro:overlap]\n        elif overlap > 0:\n            canvas[start:start + overlap] = images[:overlap]\n\n        if int(images.shape[0]) > overlap:\n            canvas[start + overlap:end] = images[overlap:]\n\n    if canvas is None:\n        raise ValueError("IAMCCS ShotboardVideoEditorRenderV1: no readable video frames found in editor manifest.")\n    return canvas.contiguous(), cross_dissolves\n\n\ndef _mix_editor_video_audio(entries: List[Tuple[Dict[str, Any], Any]], frame_count: int, fps: float) -> Dict[str, Any] | None:\n    """Mix editor audio with equal-power/dip/continuity transition envelopes."""\n    audio_entries = []\n    target_rate = 0\n    target_channels = 1\n    for clip, comp in entries:\n        waveform, sample_rate = _audio_waveform(comp.audio)\n        if waveform is None:\n            continue\n        target_rate = target_rate or sample_rate\n        target_channels = max(target_channels, int(waveform.shape[-2]))\n        audio_entries.append((clip, waveform, sample_rate))\n\n    if not audio_entries:\n        return None\n\n    target_samples = max(1, int(round(frame_count / max(1.0, fps) * target_rate)))\n    mixed = torch.zeros((1, target_channels, target_samples), dtype=torch.float32)\n\n    for clip, waveform, sample_rate in audio_entries:\n        if sample_rate != target_rate:\n            waveform = torchaudio.functional.resample(waveform, sample_rate, target_rate)\n        waveform = _normalize_audio_channels(waveform, target_channels).to(dtype=torch.float32, device=mixed.device)\n\n        destination = max(0, int(round(_safe_float(clip.get("startTime"), 0.0) * target_rate)))\n        count = min(int(waveform.shape[-1]), target_samples - destination)\n        if count <= 0:\n            continue\n\n        piece = waveform[..., :count].clone()\n        incoming = _editor_transition(clip, "in")\n        transition_type = str(incoming.get("type") or "cut").strip().lower()\n        audio_mode = str(incoming.get("audio_mode") or incoming.get("audio_curve") or "equal_power").strip().lower()\n        transition_frames = _iamccs_transition_frame_count(incoming, fps, 1)\n        transition_samples = max(1, int(round((transition_frames / max(1.0, fps)) * target_rate)))\n\n        if transition_type in {"dissolve", "continuity"}:\n            fade_samples = min(count, transition_samples, max(0, target_samples - destination))\n            if fade_samples > 0:\n                if audio_mode == "hard_cut" or (\n                    transition_type == "continuity" and audio_mode not in {"equal_power", "soft_av", "follow_video"}\n                ):\n                    mixed[..., destination:destination + fade_samples] = 0.0\n                else:\n                    outgoing_gain = _iamccs_audio_curve(\n                        fade_samples, mixed.device, mixed.dtype, incoming=False, mode=audio_mode\n                    ).view(1, 1, -1)\n                    incoming_gain = _iamccs_audio_curve(\n                        fade_samples, piece.device, piece.dtype, incoming=True, mode=audio_mode\n                    ).view(1, 1, -1)\n                    mixed[..., destination:destination + fade_samples] *= outgoing_gain\n                    piece[..., :fade_samples] *= incoming_gain\n\n        elif transition_type in {"dip_black", "dip_white"}:\n            total_samples = min(max(2, transition_samples), target_samples)\n            out_count = max(1, total_samples // 2)\n            in_count = max(1, total_samples - out_count)\n\n            actual_out = min(out_count, destination)\n            if actual_out > 0:\n                gain_out = _iamccs_audio_curve(\n                    actual_out, mixed.device, mixed.dtype, incoming=False,\n                    mode="equal_power" if audio_mode == "soft_av" else audio_mode\n                ).view(1, 1, -1)\n                mixed[..., destination - actual_out:destination] *= gain_out\n\n            actual_in = min(in_count, count)\n            if actual_in > 0:\n                gain_in = _iamccs_audio_curve(\n                    actual_in, piece.device, piece.dtype, incoming=True,\n                    mode="equal_power" if audio_mode == "soft_av" else audio_mode\n                ).view(1, 1, -1)\n                piece[..., :actual_in] *= gain_in\n\n        mixed[..., destination:destination + count] += piece\n\n    return {"waveform": mixed.clamp(-1.0, 1.0), "sample_rate": int(target_rate)}\n'

def fail(message: str) -> None:
    raise SystemExit("ERROR: " + message + "\nNessun file è stato scritto.")

def replace_once(text: str, old: str, new: str, label: str) -> str:
    count = text.count(old)
    if count != 1:
        fail(f"{label}: attesa 1 signature, trovate {count}.")
    return text.replace(old, new, 1)

if not UI.is_file():
    fail(f"Video Editor UI non trovato: {UI}")
if not BACKEND.is_file():
    fail(f"Backend Video Editor non trovato: {BACKEND}")

ui_text = UI.read_text(encoding="utf-8-sig")
backend_text = BACKEND.read_text(encoding="utf-8-sig")

if MARKER in ui_text and MARKER in backend_text:
    print("IAMCCS TRANSITIONS PRO v1 risulta già installato.")
    raise SystemExit(0)

new_ui = ui_text
new_backend = backend_text

if MARKER not in new_ui:
    insert_anchor = '  function resetEditorProject(reason = "clear") {'
    if new_ui.count(insert_anchor) != 1:
        fail(f"UI insert anchor: attesa 1 signature, trovate {new_ui.count(insert_anchor)}.")
    new_ui = new_ui.replace(insert_anchor, UI_INSERT + "\n" + insert_anchor, 1)

    button_anchor = """  takesActions.append(
    linkButton,
    makeButton("Add Video", (button) => addManual("video", button), "gold"),"""
    button_replacement = """  takesActions.append(
    linkButton,
    createTransitionsProButton(),
    makeButton("Add Video", (button) => addManual("video", button), "gold"),"""
    new_ui = replace_once(new_ui, button_anchor, button_replacement, "UI TRANS button anchor")

if MARKER not in new_backend:
    start_sig = "def _assemble_editor_timeline_frames("
    mix_sig = "def _mix_editor_video_audio("
    end_sig = "\n\nclass IAMCCS_ShotboardVideoEditorRenderV1:"
    start = new_backend.find(start_sig)
    mix = new_backend.find(mix_sig, start + 1 if start >= 0 else 0)
    end = new_backend.find(end_sig, mix + 1 if mix >= 0 else 0)
    if start < 0 or mix < 0 or end < 0 or not (start < mix < end):
        fail(
            "Backend transition block non riconosciuto. "
            f"assemble={start}, mix={mix}, render_class={end}."
        )
    new_backend = new_backend[:start] + BACKEND_BLOCK.strip() + new_backend[end:]

required_ui = [
    "IAMCCS_TRANSITIONS_PRO_V1",
    "createTransitionsProButton",
    "Continuity Join",
    "Dip to Black",
    "timeline_shift_policy",
]
for item in required_ui:
    if item not in new_ui:
        fail(f"Validazione UI: marker mancante {item!r}.")

required_backend = [
    "IAMCCS_TRANSITIONS_PRO_V1",
    "_iamccs_transition_curve",
    'transition_type == "continuity"',
    'transition_type in {"dip_black", "dip_white"}',
    "_iamccs_audio_curve",
]
for item in required_backend:
    if item not in new_backend:
        fail(f"Validazione backend: marker mancante {item!r}.")

if new_ui != ui_text and not UI_BACKUP.exists():
    shutil.copy2(UI, UI_BACKUP)
    print(f"Backup creato: {UI_BACKUP}")
if new_backend != backend_text and not BACKEND_BACKUP.exists():
    shutil.copy2(BACKEND, BACKEND_BACKUP)
    print(f"Backup creato: {BACKEND_BACKUP}")

if new_ui != ui_text:
    UI.write_text(new_ui, encoding="utf-8")
    print(f"Modificato: {UI}")
if new_backend != backend_text:
    BACKEND.write_text(new_backend, encoding="utf-8")
    print(f"Modificato: {BACKEND}")

try:
    py_compile.compile(str(BACKEND), doraise=True)
except Exception as exc:
    if UI_BACKUP.exists():
        shutil.copy2(UI_BACKUP, UI)
    if BACKEND_BACKUP.exists():
        shutil.copy2(BACKEND_BACKUP, BACKEND)
    raise SystemExit(
        "ERROR: py_compile backend fallito; rollback automatico eseguito.\n"
        f"{exc}"
    )

print()
print("IAMCCS VIDEO EDITOR TRANSITIONS PRO v1 APPLICATO")
print("UI: un solo pulsante TRANS apre un box flottante; layout principale invariato.")
print("Video: Cut / Cross Dissolve / Dip to Black / Dip to White / Continuity Join.")
print("Audio: Equal Power / Follow Video / Hard Cut / Soft AV.")
print("Continuity Join: overlap incoming-owned + micro blend configurabile.")
print()
print("Riavvia ComfyUI e fai Ctrl+F5.")
print()
print("ROLLBACK MANUALE:")
print(f'  copy /Y "{UI_BACKUP}" "{UI}"')
print(f'  copy /Y "{BACKEND_BACKUP}" "{BACKEND}"')

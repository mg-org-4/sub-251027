# Core dependencies for ComfyUI-Gemini
httpx>=0.25.0
orjson>=3.9.0
loguru>=0.7.0
Pillow>=9.0.0
pydantic>=2.0.0

# Windows-specific for cookie extraction
pywin32>=306; sys_platform == "win32"
pycryptodomex>=3.20.0; sys_platform == "win32"

# Optional: For v20 cookie decryption (requires admin)
PythonForWindows>=1.0.0; sys_platform == "win32"

# Optional fallback for non-Windows
browser-cookie3>=0.19.0

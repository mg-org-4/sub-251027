name: Nightly Build

on:
  push:
    branches:
      - main
      - master
      - nightly
    paths-ignore:
      - '**.md'
      - '.github/workflows/**'
      - 'tests/**'

permissions: {}

jobs:
  build-nightly:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Create Archive
        # Nous reproduisons ici la logique d'exclusion de votre script make_release_zip.ps1
        run: |
          echo "nightly" > .mjr_channel
          zip -r ComfyUI-Majoor-AssetsManager_nightly.zip . \
          -x "*.git/*" \
          -x ".github/*" \
          -x ".claude/*" \
          -x "*/__pycache__/*" \
          -x "node_modules/*" \
          -x "*/.pytest_cache/*" \
          -x "tests/*" \
          -x "scripts/make_release_zip.ps1" \
          -x "coverage_*.json" \
          -x "audit_findings.md" \
          -x ".vscode/*" \
          -x "*.zip"

      - name: Update Nightly Release
        uses: softprops/action-gh-release@v3 # TODO: pin to commit SHA for supply-chain safety
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: nightly
          name: "Nightly Build (Development)"
          prerelease: true
          overwrite_files: true
          files: |
            ./ComfyUI-Majoor-AssetsManager_nightly.zip

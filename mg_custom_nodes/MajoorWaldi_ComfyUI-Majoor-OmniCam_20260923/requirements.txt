# Intentionally empty.
#
# Runtime Python dependencies required by the Registry are declared in
# pyproject.toml. OmniCam otherwise uses ComfyUI's bundled Python/aiohttp APIs.
#
# The generated frontend bundle is committed, so a raw Git source checkout
# works as-is. Contributors changing web-src/ should run `npm ci` followed by
# `npm run build` to regenerate it before committing.
#
# Development and CI tooling lives in requirements-dev.txt.

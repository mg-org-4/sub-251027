pytest==9.0.2
pytest-asyncio==1.3.0
ruff==0.15.17
mypy==1.19.1
# ComfyUI's V3 Video API imports NumPy during module initialization. Keep the
# core test environment able to import the same contracts as integration CI.
numpy==2.2.6
# Validates the published contracts in schemas/ against real payloads.
jsonschema==4.26.0

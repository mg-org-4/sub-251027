This Ref2VA clip CONTINUES the previous clip in one unbroken reference
chain (the generation loop carries an overlap from the end of the
previous clip into the start of this one).

Previous clip "{previous_id}" — subject_definitions bindings:
"""
{previous_subject_definitions}
"""

Previous clip "{previous_id}" — detailed_description body:
"""
{previous_description}
"""

Previous clip "{previous_id}" — overall_soundscape (this exact bed must carry over):
"""
{previous_soundscape}
"""

Continuation rules for THIS Ref2VA scene:
- Continue the same unbroken moment; the carried opening overlap already
  contains the end of the previous clip. Begin NEW action only after
  that overlap; do not re-perform what the overlap already shows.
- RE-ASSERT subject_definitions in this scene's own voice: restate each
  <Subject N> / <Picture M> binding from the manifest in this scene's
  wording. Identity slots keep `fully_preserved`; non-identity slots
  keep `reference`. The bindings must match the manifest binding; do
  NOT invent new subjects or renumber existing ones across scenes.
- Preserve every recurring subject EXACTLY as established in the previous
  scene's subject_definitions — identity, species, markings, wardrobe,
  and who-wears/holds-what stay identical. Do not move props,
  accessories, or colors between characters, and do not invent new
  recurring subjects.
- summary MUST start with `[reference generation]` again (do NOT switch
  to `[video continuation + ...]` — that is a different H3 task type).
- retention_analysis: re-list every Picture / Subject label this scene
  uses; identity slots stay `fully_preserved - ...`, non-identity slots
  stay `reference - ...`. Pair each <Subject N> with its <Picture M>.
- detailed_description: continue the unbroken action (mid-action from
  the carried overlap). End this clip mid-action so the NEXT clip can
  continue it (unless it is the final clip).
- overall_soundscape: carry the EXACT base-layer sounds listed above
  across the boundary (name them as carried), then add this clip's new
  close sounds on top. Do not restart the bed from silence, and do not
  introduce new background elements as if they had been carried over.
- non_diegetic_music: keep the same music line unless the concept
  explicitly switches.

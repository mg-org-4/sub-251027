repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Ruff version (keep in sync with the 'ruff' dev dependency / venv).
    rev: v0.11.8
    hooks:
      # Run the linter.
      - id: ruff
        args: [ --fix ]
      # Run the formatter.
      - id: ruff-format

<!-- This file was auto-translated with a local LLM and last updated on 2025-12-27. -->
## 輸出清單組合

![輸出清單組合](CombineOutputLists/CombineOutputLists.png)

(ComfyUI 工作流程已包含)

可接受最多 4 個輸出清單，並生成它們的所有組合。

範例：`[1, 2, 3] x ["A", "B"] = [(1, "A"), (1, "B"), (2, "A"), (2, "B"), (3, "A"), (3, "B")]`

`unzip_a` 至 `unzip_d` 使用 `is_output_list=True`（以符號 `𝌠` 表示），並將由對應節點順序處理。

所有清單皆為可選，空清單將被忽略。

技術上來說，它計算的是「笛卡爾積」，並將每組組合拆解為其元素（`unzip`），而空清單將被替換為 `None`，並在對應輸出上發出 `None`。

範例：`[1, 2] x [] x ["A", "B"] x [] = [(1, None, "A", None), (1, None, "B", None), (2, None, "A", None), (2, None, "B", None)]`

### 輸入

| 名稱 | 類型 | 描述 |
| --- | --- | --- |
| `list_a` | `*` | （可選）|
| `list_b` | `*` | （可選）|
| `list_c` | `*` | （可選）|
| `list_d` | `*` | （可選）|

### 輸出

| 名稱 | 類型 | 描述 |
| --- | --- | --- |
| `unzip_a` | `* 𝌠` | 對應於 `list_a` 的組合值。|
| `unzip_b` | `* 𝌠` | 對應於 `list_b` 的組合值。|
| `unzip_c` | `* 𝌠` | 對應於 `list_c` 的組合值。|
| `unzip_d` | `* 𝌠` | 對應於 `list_d` 的組合值。|
| `index` | `INT 𝌠` | 0 到 count 的範圍，可用作索引。|
| `count` | `INT` | 組合總數。|


<!-- This file was auto-translated with a local LLM and last updated on 2025-12-27. -->
## Комбинации OutputLists

![Комбинации OutputLists](CombineOutputLists/CombineOutputLists.png)

(включён рабочий процесс ComfyUI)

Принимает до 4 списков OutputLists и генерирует все возможные комбинации между ними.

Пример: `[1, 2, 3] x ["A", "B"] = [(1, "A"), (1, "B"), (2, "A"), (2, "B"), (3, "A"), (3, "B")]`

Параметры `unzip_a` .. `unzip_d` используют `is_output_list=True` (обозначено символом `𝌠`) и будут обрабатываться последовательно соответствующими узлами.

Все списки необязательны, пустые списки будут проигнорированы.

Технически вычисляется *декартово произведение* и каждая комбинация выводится, разбитая на отдельные элементы (`unzip`), при этом пустые списки заменяются на `None`, и на соответствующий выход будет выдаваться `None`.

Пример: `[1, 2] x [] x ["A", "B"] x [] = [(1, None, "A", None), (1, None, "B", None), (2, None, "A", None), (2, None, "B", None)]`

### Входы

| Имя | Тип | Описание |
| --- | --- | --- |
| `list_a` | `*` | (необязательно) |
| `list_b` | `*` | (необязательно) |
| `list_c` | `*` | (необязательно) |
| `list_d` | `*` | (необязательно) |

### Выходы

| Имя | Тип | Описание |
| --- | --- | --- |
| `unzip_a` | `* 𝌠` | Значение комбинаций, соответствующих `list_a`. |
| `unzip_b` | `* 𝌠` | Значение комбинаций, соответствующих `list_b`. |
| `unzip_c` | `* 𝌠` | Значение комбинаций, соответствующих `list_c`. |
| `unzip_d` | `* 𝌠` | Значение комбинаций, соответствующих `list_d`. |
| `index` | `INT 𝌠` | Диапазон от 0 до количества, который можно использовать как индекс. |
| `count` | `INT` | Общее количество комбинаций. |


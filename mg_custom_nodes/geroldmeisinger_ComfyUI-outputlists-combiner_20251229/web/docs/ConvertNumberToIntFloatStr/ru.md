<!-- This file was auto-translated with a local LLM and last updated on 2025-12-28. -->
## Преобразовать в int, float, str

![Convert To Int Float Str](ConvertNumberToIntFloatStr/ConvertNumberToIntFloatStr.png)

(Включён рабочий процесс ComfyUI)

Преобразует любые значения, похожие на числа, в `INT`, `FLOAT`, `STRING`.
Внутренне использует `nums_from_string.get_nums`, который очень гибок в принятии чисел. Поддерживаются настоящие целые числа, настоящие дробные числа, целые или дробные числа в виде строк, строки, содержащие несколько чисел с разделителями тысяч.
Используйте строку `123;234;345`, чтобы быстро создать список чисел. Не используйте запятые в качестве разделителей, так как они могут быть интерпретированы как разделители тысяч.
Параметры `int`, `float` и `string` используют `is_output_list=True` (обозначено символом `𝌠`) и будут обрабатываться последовательно соответствующими узлами.

### Входы

| Имя | Тип | Описание |
| --- | --- | --- |
| `any` | `*` | Любое значение, которое можно осмысленно преобразовать в строку с парсабельными числами внутри |

### Выходы

| Имя | Тип | Описание |
| --- | --- | --- |
| `int` | `INT 𝌠` | Все числа, найденные в строке, с отброшенными десятичными дробями. |
| `float` | `FLOAT 𝌠` | Все числа, найденные в строке, в виде дробных чисел. |
| `string` | `STRING 𝌠` | Все числа, найденные в строке, в виде дробных чисел, преобразованных в строку. |
| `count` | `INT` | Количество чисел, найденных в значении. |


<!-- This file was auto-translated with a local LLM and last updated on 2025-12-27. -->
## مجموعات إخراج القوائم

![مجموعات إخراج القوائم](CombineOutputLists/CombineOutputLists.png)

(مخطط عمل ComfyUI مُدرج)

يأخذ حتى 4 قوائم إخراج ويعمل على كل تشكيل منها.

مثال: `[1, 2, 3] x ["A", "B"] = [(1, "A"), (1, "B"), (2, "A"), (2, "B"), (3, "A"), (3, "B")]`

`unzip_a` .. `unzip_d` يستخدمون `is_output_list=True` (مُشار إليه بالرمز `𝌠`) وستُعالج تباعًا من قبل العقد المقابلة.

جميع القوائم اختيارية، وستُتجاهل القوائم الفارغة.

بشكل تقني، يحسب *المنتج الكارتيزي* ويُخرج كل تشكيل مُقسّم إلى عناصره (`unzip`)، بينما ستُستعاض القوائم الفارغة بقيم `None`، وستُرسل `None` على المخرج المقابل.

مثال: `[1, 2] x [] x ["A", "B"] x [] = [(1, None, "A", None), (1, None, "B", None), (2, None, "A", None), (2, None, "B", None)]`

### المدخلات

| الاسم | النوع | الوصف |
| --- | --- | --- |
| `list_a` | `*` | (اختياري) |
| `list_b` | `*` | (اختياري) |
| `list_c` | `*` | (اختياري) |
| `list_d` | `*` | (اختياري) |

### المخرجات

| الاسم | النوع | الوصف |
| --- | --- | --- |
| `unzip_a` | `* 𝌠` | القيم المُستمدة من التشكيلات المرتبطة بـ `list_a`. |
| `unzip_b` | `* 𝌠` | القيم المُستمدة من التشكيلات المرتبطة بـ `list_b`. |
| `unzip_c` | `* 𝌠` | القيم المُستمدة من التشكيلات المرتبطة بـ `list_c`. |
| `unzip_d` | `* 𝌠` | القيم المُستمدة من التشكيلات المرتبطة بـ `list_d`. |
| `index` | `INT 𝌠` | نطاق من 0 إلى عدد التشكيلات يمكن استخدامه كمُؤشر. |
| `count` | `INT` | العدد الكلي للتشكيلات. |


<!-- This file was auto-translated with a local LLM and last updated on 2025-12-27. -->
## OutputLists 组合

![OutputLists Combinations](CombineOutputLists/CombineOutputLists.png)

(包含 ComfyUI 工作流)

接收最多 4 个 OutputLists 并生成它们的所有组合。

示例：`[1, 2, 3] x ["A", "B"] = [(1, "A"), (1, "B"), (2, "A"), (2, "B"), (3, "A"), (3, "B")]`

`unzip_a` .. `unzip_d` 使用 `is_output_list=True`（由符号 `𝌠` 表示），并将由对应节点按顺序处理。

所有列表均为可选，空列表将被忽略。

从技术上讲，它计算的是 *笛卡尔积*，并将每个组合拆分为其元素（`unzip`），而空列表将被替换为 `None`，并在相应输出上发出 `None`。

示例：`[1, 2] x [] x ["A", "B"] x [] = [(1, None, "A", None), (1, None, "B", None), (2, None, "A", None), (2, None, "B", None)]`

### 输入

| 名称 | 类型 | 描述 |
| --- | --- | --- |
| `list_a` | `*` | (可选) |
| `list_b` | `*` | (可选) |
| `list_c` | `*` | (可选) |
| `list_d` | `*` | (可选) |

### 输出

| 名称 | 类型 | 描述 |
| --- | --- | --- |
| `unzip_a` | `* 𝌠` | 与 `list_a` 对应的组合值。 |
| `unzip_b` | `* 𝌠` | 与 `list_b` 对应的组合值。 |
| `unzip_c` | `* 𝌠` | 与 `list_c` 对应的组合值。 |
| `unzip_d` | `* 𝌠` | 与 `list_d` 对应的组合值。 |
| `index` | `INT 𝌠` | 范围为 0..count，可用作索引。 |
| `count` | `INT` | 组合总数。 |

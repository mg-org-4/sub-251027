<!-- This file was auto-translated with a local LLM and last updated on 2025-12-28. -->
## تبدیل به عدد صحیح، اعشاری، رشته

![تبدیل به عدد صحیح، اعشاری، رشته](ConvertNumberToIntFloatStr/ConvertNumberToIntFloatStr.png)

(فرآیند کار ComfyUI همراه است)

هر چیزی که شبیه عدد باشد را به `عدد صحیح` `اعشاری` `رشته` تبدیل می‌کند.
به صورت داخلی از `nums_from_string.get_nums` استفاده می‌کند که در قبول اعداد بسیار آزاد است. هر چیزی از اعداد صحیح واقعی، اعشاری واقعی، اعداد صحیح یا اعشاری به صورت رشته، رشته‌هایی که چندین عدد با جداکننده‌های هزارگان دارند.
از یک رشته `123;234;345` برای سریع‌تر تولید لیست اعداد استفاده کنید. از کاما به عنوان جداکننده استفاده نکنید زیرا ممکن است به عنوان جداکننده هزارگان تفسیر شوند.
`عدد صحیح`، `اعشاری` و `رشته` از `is_output_list=True` استفاده می‌کنند (با نماد `𝌠` نشان داده شده) و به صورت متوالی توسط گره‌های مربوطه پردازش خواهند شد.

### ورودی‌ها

| نام | نوع | توضیحات |
| --- | --- | --- |
| `هرچیزی` | `*` | هر چیزی که بتوان به صورت معنادار به رشته تبدیل کرد که شامل اعداد قابل تحلیل باشد |

### خروجی‌ها

| نام | نوع | توضیحات |
| --- | --- | --- |
| `عدد صحیح` | `عدد صحیح 𝌠` | تمام اعداد یافت شده در رشته با حذف اعشار. |
| `اعشاری` | `اعشاری 𝌠` | تمام اعداد یافت شده در رشته به صورت اعشاری. |
| `رشته` | `رشته 𝌠` | تمام اعداد یافت شده در رشته به صورت اعشاری و سپس به رشته تبدیل شده. |
| `تعداد` | `عدد صحیح` | تعداد اعداد یافت شده در مقدار. |


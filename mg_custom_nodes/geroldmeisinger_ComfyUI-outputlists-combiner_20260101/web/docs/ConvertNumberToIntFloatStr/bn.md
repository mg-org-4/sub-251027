## ইন্ট ফ্লোট স্ট্রিং এ রূপান্তর করুন

![Convert To Int Float Str](ConvertNumberToIntFloatStr/ConvertNumberToIntFloatStr.png)

(ComfyUI workflow সহ)

যেকোনো নাম্বার-এর মতো কিছুকে `INT` `FLOAT` `STRING` এ রূপান্তর করে। এটি বেসিক ভাবে `nums_from_string.get_nums` ব্যবহার করে যা নাম্বারগুলোকে খুব সহজে গ্রহণ করে। ইন্টিজার, ফ্লোট, স্ট্রিং হিসেবে ইন্টিজার বা ফ্লোট এর মতো যেকোনো কিছু গ্রহণ করে। স্ট্রিং যেমন `123;234;345` ব্যবহার করে সহজে নাম্বারের তালিকা তৈরি করুন। কমা ব্যবহার করবেন না কারণ এগুলো হয়তো থাউসেন্ড স্পেস হিসেবে বুঝতে পারে।
`int`, `float` এবং `string` এর জন্য `is_output_list=True` (প্রতীক `𝌠` দ্বারা চিহ্নিত) ব্যবহার করা হয় এবং সংশ্লিষ্ট নোড দ্বারা ক্রমান্বয়ে প্রসেস করা হয়।

### ইনপুট

| নাম | টাইপ | বর্ণনা |
| --- | --- | --- |
| `any` | `*` | যেকোনো কিছু যা স্ট্রিং হিসেবে রূপান্তর করা যায় এবং তাতে পাওয়া যায় নাম্বার |

### আউটপুট

| নাম | টাইপ | বর্ণনা |
| --- | --- | --- |
| `int` | `INT 𝌠` | স্ট্রিং থেকে পাওয়া সব নাম্বার দশমিক ছাড়া। |
| `float` | `FLOAT 𝌠` | স্ট্রিং থেকে পাওয়া সব নাম্বার ফ্লোট হিসেবে। |
| `string` | `STRING 𝌠` | স্ট্রিং থেকে পাওয়া সব নাম্বার ফ্লোট হিসেবে রূপান্তর করে স্ট্রিং হিসেবে। |
| `count` | `INT` | মান থেকে পাওয়া নাম্বারের সংখ্যা। |


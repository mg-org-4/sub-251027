## JSON OutputList

![JSON OutputList](JSONOutputList/JSONOutputList.png)

(ComfyUI workflow সহ)

JSON অবজেক্ট থেকে অ্যারে বা ডিকশনারি এক্সট্রাক্ট করে OutputList তৈরি করে।
মান এক্সট্রাক্ট করার জন্য JSONPath সিনট্যাক্স ব্যবহার করে, [JSONPath on Wikipedia](https://en.wikipedia.org/wiki/JSONPath) দেখুন।
সব মিলে গেল মান একটি দীর্ঘ তালিকায় ফ্ল্যাট করা হয়।
আপনি এই নোডটি ব্যবহার করে লিটারাল স্ট্রিং থেকে অবজেক্ট তৈরি করতে পারেন যেমন `[1, 2, 3]`।
`key`, `value`, `int` এবং `float` ব্যবহার করে `is_output_list=True` (সিম্বল `𝌠` দ্বারা চিহ্নিত) এবং তারা সংশ্লিষ্ট নোড দ্বারা ক্রমান্বয়ে প্রসেস হবে।

### ইনপুট

| নাম | টাইপ | বর্ণনা |
| --- | --- | --- |
| `jsonpath` | `STRING` | মান এক্সট্রাক্ট করার জন্য ব্যবহৃত JSONPath। |
| `json` | `STRING` | একটি JSON স্ট্রিং যা অবজেক্টে রূপান্তরিত হয়। |
| `obj` | `*` | (ঐচ্ছিক) যেকোনো ধরনের অবজেক্ট যা JSON স্ট্রিং প্রতিস্থাপন করবে |

### আউটপুট

| নাম | টাইপ | বর্ণনা |
| --- | --- | --- |
| `key` | `STRING 𝌠` | ডিকশনারির জন্য কি বা অ্যারের জন্য ইনডেক্স (স্ট্রিং হিসেবে)। প্রায়শই এটি ফ্ল্যাট করা তালিকার জন্য একটি গ্লোবাল ইনডেক্স। |
| `value` | `STRING 𝌠` | মান স্ট্রিং হিসেবে। |
| `int` | `INT 𝌠` | মান ইন্টিজার হিসেবে (যদি সংখ্যা পার্স করতে না পারে তবে ডিফল্ট 0 হয়)। |
| `float` | `FLOAT 𝌠` | মান ফ্লোট হিসেবে (যদি সংখ্যা পার্স করতে না পারে তবে ডিফল্ট 0 হয়)। |
| `count` | `INT` | ফ্ল্যাট করা তালিকার মোট আইটেম সংখ্যা |
| `debug` | `STRING` | ফরমেটেড JSON স্ট্রিং হিসেবে সব মিলে গেল অবজেক্টের ডিবাগ আউটপুট |


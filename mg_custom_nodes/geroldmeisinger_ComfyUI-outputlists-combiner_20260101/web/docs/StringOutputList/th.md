## String OutputList

![String OutputList](StringOutputList/StringOutputList.png)

(ComfyUI workflow ที่รวมอยู่)

สร้าง OutputList โดยการแยกสตริงในช่องข้อความด้วยตัวคั่น
`value` และ `index` ใช้ `is_output_list=True` (แสดงโดยสัญลักษณ์ `𝌠`) และจะถูกประมวลผลตามลำดับโดยโหนดที่สอดคล้องกัน

### ป้อนข้อมูล

| ชื่อ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| `separator` | `STRING` | สตริงที่ใช้ในการแยกค่าจากช่องข้อความ |
| `values` | `STRING` | ข้อความที่คุณต้องการแยกเป็นรายการ หมายเหตุว่าสตริงจะถูกตัดช่องว่างที่อยู่ท้ายก่อนการแยก และแต่ละรายการจะถูกตัดช่องว่างที่อยู่ทั้งสองข้างอีกครั้ง |

### ผลลัพธ์

| ชื่อ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| `value` | `* 𝌠` | ค่าจากรายการ |
| `index` | `INT 𝌠` | ช่วงของ 0..count คุณสามารถใช้สิ่งนี้เป็นดัชนี |
| `count` | `INT` | จำนวนรายการในรายการ |
| `inspect_combo` | `COMBO` | ผลลัพธ์จำลองที่คุณสามารถเชื่อมโยงกับ `COMBO` และเติมค่าจากมันได้ เชื่อมต่อจะถูกเชื่อมโยงกลับไปยังผลลัพธ์ `value` โดยอัตโนมัติ |


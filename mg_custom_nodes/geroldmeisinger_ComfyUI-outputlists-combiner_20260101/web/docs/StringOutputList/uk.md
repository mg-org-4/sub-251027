## String OutputList

![String OutputList](StringOutputList/StringOutputList.png)

(Включено ComfyUI workflow)

Створює OutputList шляхом розбиття рядка в текстовому полі роздільником.
`value` та `index` використовують `is_output_list=True` (позначено символом `𝌠`) і будуть оброблені послідовно відповідними вузлами.

### Вхідні дані

| Назва | Тип | Опис |
| --- | --- | --- |
| `separator` | `STRING` | Рядок, який використовується для розділення значень текстового поля. |
| `values` | `STRING` | Текст, який ви хочете розділити на список. Зауважте, що рядок обрізається від зайвих символів нового рядка перед розділенням, а кожен елемент також обрізається від пробілів. |

### Вихідні дані

| Назва | Тип | Опис |
| --- | --- | --- |
| `value` | `* 𝌠` | Значення зі списку. |
| `index` | `INT 𝌠` | Діапазон від 0..count. Ви можете використовувати це як індекс. |
| `count` | `INT` | Кількість елементів у списку. |
| `inspect_combo` | `COMBO` | Фіктивний вихід, який ви можете використовувати для підключення до `COMBO` і заповнення його значеннями. Підключення буде автоматично перепідключено до виходу `value`. |


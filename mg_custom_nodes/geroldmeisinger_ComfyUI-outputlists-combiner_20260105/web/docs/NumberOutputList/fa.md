## لیست خروجی عددی

![Number OutputList](NumberOutputList/NumberOutputList.png)

(فایل کارگاه ComfyUI همراه داده شده است)

یک لیست خروجی با محدوده‌ای از مقادیر عددی ایجاد می‌کند.
از [numpy.linspace](https://numpy.org/doc/stable/reference/generated/numpy.linspace.html) به صورت داخلی استفاده می‌کند، چون با مقادیر اعشاری کارایی بیشتری دارد.
اگر می‌خواهید لیست‌های عددی با گام‌های دلخواه تعریف کنید، لیست خروجی JSON را بررسی کنید و یک آرایه تعریف کنید، مثلاً `[1, 42, 123]`.
`int`, `float`, `string` و `index` از `is_output_list=True` (که با نماد `𝌠` نشان داده شده است) استفاده می‌کنند و به صورت متوالی توسط نودهای مربوطه پردازش می‌شوند.

### ورودی‌ها

| نام | نوع | توضیح |
| --- | --- | --- |
| `start` | `FLOAT` | مقدار شروع برای تولید محدوده. |
| `stop` | `FLOAT` | مقدار پایان. اگر `endpoint=include` باشد، این عدد در لیست شامل می‌شود. |
| `num` | `INT` | تعداد آیتم‌های لیست (با گام تفاوت نداشته باشد). |
| `endpoint` | `BOOLEAN` | تصمیم می‌گیرد که مقدار `stop` در آیتم‌ها شامل شود یا نشود. |

### خروجی‌ها

| نام | نوع | توضیح |
| --- | --- | --- |
| `int` | `INT 𝌠` | مقدار تبدیل شده به int (گرد شده به پایین/کاهش یافته). |
| `float` | `FLOAT 𝌠` | مقدار به صورت اعشاری. |
| `string` | `STRING 𝌠` | مقدار به صورت اعشاری تبدیل شده به رشته. |
| `index` | `INT 𝌠` | محدوده 0..count که می‌تواند به عنوان شاخص استفاده شود. |
| `count` | `INT` | همانند `num`. |


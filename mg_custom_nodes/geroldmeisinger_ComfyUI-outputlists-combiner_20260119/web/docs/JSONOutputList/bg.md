## JSON Изходен Списък

![JSON OutputList](JSONOutputList/JSONOutputList.png)

(Включва ComfyUI workflow)

Създава OutputList, като извлича масиви или речници от JSON обекти.
Използва синтаксиса на JSONPath за извличане на стойностите, виж [JSONPath в Уикипедия](https://en.wikipedia.org/wiki/JSONPath) .
Всички съвпадащи стойности се изравняват в един дълъг списък.
Можете също така да използвате този node за създаване на обекти от буквени низове като `[1, 2, 3]`.
`key`, `value`, `int` и `float` използват `is_output_list=True` (обозначени със символа `𝌠`) и ще бъдат обработени последователно от съответните node-и.

### Входове

| Име | Тип | Описание |
| --- | --- | --- |
| `jsonpath` | `STRING` | JSONPath, използван за извличане на стойностите. |
| `json` | `STRING` | JSON низ, който се превръща в обект. |
| `obj` | `*` | (по избор) обект от който и да е тип, който ще замени JSON низа |

### Изходи

| Име | Тип | Описание |
| --- | --- | --- |
| `key` | `STRING 𝌠` | Ключът за речници или индексът за масиви (като низ). Технически това е глобален индекс на изравнения списък за всички не-ключове. |
| `value` | `STRING 𝌠` | Стойността като низ. |
| `int` | `INT 𝌠` | Стойността като цяло число (ако не може да се анализира числото, по подразбиране е 0). |
| `float` | `FLOAT 𝌠` | Стойността като десетично число (ако не може да се анализира числото, по подразбиране е 0). |
| `count` | `INT` | Общ брой елементи в изравнения списък |
| `debug` | `STRING` | Debug изход на всички съвпадащи обекти като форматиран JSON низ |


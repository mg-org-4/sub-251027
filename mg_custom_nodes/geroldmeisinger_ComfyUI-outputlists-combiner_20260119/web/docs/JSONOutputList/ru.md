## JSON OutputList

![JSON OutputList](JSONOutputList/JSONOutputList.png)

(ComfyUI workflow включен)

Создает OutputList, извлекая массивы или словари из JSON-объектов.
Использует синтаксис JSONPath для извлечения значений, см. [JSONPath на Wikipedia](https://en.wikipedia.org/wiki/JSONPath).
Все совпадающие значения сворачиваются в один длинный список.
Вы также можете использовать этот узел для создания объектов из строковых литералов, таких как `[1, 2, 3]`.
`key`, `value`, `int` и `float` используют `is_output_list=True` (обозначено символом `𝌠`) и будут обрабатываться последовательно соответствующими узлами.

### Входы

| Имя | Тип | Описание |
| --- | --- | --- |
| `jsonpath` | `STRING` | JSONPath, используемый для извлечения значений. |
| `json` | `STRING` | Строка JSON, которая преобразуется в объект. |
| `obj` | `*` | (необязательно) объект любого типа, который заменит строку JSON |

### Выходы

| Имя | Тип | Описание |
| --- | --- | --- |
| `key` | `STRING 𝌠` | Ключ для словарей или индекс для массивов (в виде строки). Технически это глобальный индекс сплющенного списка для всех не-ключей. |
| `value` | `STRING 𝌠` | Значение в виде строки. |
| `int` | `INT 𝌠` | Значение в виде целого числа (если не удается разобрать число, по умолчанию 0). |
| `float` | `FLOAT 𝌠` | Значение в виде числа с плавающей точкой (если не удается разобрать число, по умолчанию 0). |
| `count` | `INT` | Общее количество элементов в сплющенном списке |
| `debug` | `STRING` | Отладочный вывод всех совпадающих объектов в виде отформатированной строки JSON |


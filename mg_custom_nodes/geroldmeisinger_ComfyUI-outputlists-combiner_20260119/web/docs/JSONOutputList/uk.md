## JSON OutputList

![JSON OutputList](JSONOutputList/JSONOutputList.png)

(ComfyUI workflow включено)

Створює OutputList шляхом витягання масивів або словників з JSON-об'єктів.
Використовує синтаксис JSONPath для витягання значень, див. [JSONPath на Wikipedia](https://en.wikipedia.org/wiki/JSONPath) .
Усі знайдені значення сплощуються в один довгий список.
Ви також можете використовувати цей вузол для створення об'єктів із літеральних рядків, таких як `[1, 2, 3]`.
`key`, `value`, `int` та `float` використовують `is_output_list=True` (позначено символом `𝌠`) і будуть оброблені послідовно відповідними вузлами.

### Входи

| Ім'я | Тип | Опис |
| --- | --- | --- |
| `jsonpath` | `STRING` | JSONPath, який використовується для витягання значень. |
| `json` | `STRING` | JSON-рядок, який перетворюється на об'єкт. |
| `obj` | `*` | (необов'язково) об'єкт будь-якого типу, який замінить JSON-рядок |

### Виходи

| Ім'я | Тип | Опис |
| --- | --- | --- |
| `key` | `STRING 𝌠` | Ключ для словників або індекс для масивів (як рядок). Технічно це глобальний індекс сплощеного списку для всіх не-ключів. |
| `value` | `STRING 𝌠` | Значення як рядок. |
| `int` | `INT 𝌠` | Значення як ціле число (якщо не вдається розпізнати число, за замовчуванням 0). |
| `float` | `FLOAT 𝌠` | Значення як дійсне число (якщо не вдається розпізнати число, за замовчуванням 0). |
| `count` | `INT` | Загальна кількість елементів у сплощеному списку |
| `debug` | `STRING` | Вивід налагодження всіх знайдених об'єктів у форматі JSON-рядка |


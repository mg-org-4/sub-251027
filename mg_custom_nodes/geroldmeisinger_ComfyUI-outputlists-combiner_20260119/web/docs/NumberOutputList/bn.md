## Number OutputList

![Number OutputList](NumberOutputList/NumberOutputList.png)

(ComfyUI workflow সহ)

সংখ্যামান মানের সীমা সহ একটি OutputList তৈরি করে।
[numpy.linspace](https://numpy.org/doc/stable/reference/generated/numpy.linspace.html) ব্যবহার করে অভ্যন্তরীণভাবে, কারণ এটি ফ্লোটিং-পয়েন্ট মানের সাথে বেশি নির্ভুলভাবে কাজ করে।
আপনি যদি বর্ণনা করা পদক্ষেপের সাথে সংখ্যা তালিকা নির্ধারণ করতে চান তবে JSON OutputList দেখুন এবং একটি অ্যারে সংজ্ঞায়িত করুন, উদাহরণস্বরূপ `[1, 42, 123]`।
`int`, `float`, `string` এবং `index` ব্যবহার করে `is_output_list=True` (প্রতীক `𝌠` দ্বারা নির্দেশিত) এবং সংশ্লিষ্ট নোড দ্বারা ক্রমান্বয়ে প্রক্রিয়া করা হবে।

### ইনপুট

| নাম | টাইপ | বর্ণনা |
| --- | --- | --- |
| `start` | `FLOAT` | সীমা তৈরির জন্য শুরুর মান। |
| `stop` | `FLOAT` | শেষ মান। যদি `endpoint=include` হয় তবে এই সংখ্যা তালিকায় অন্তর্ভুক্ত হবে। |
| `num` | `INT` | তালিকার আইটেমের সংখ্যা (এটি `step` এর সাথে মিলিয়ে না মিলিয়ে বুঝুন)। |
| `endpoint` | `BOOLEAN` | সিদ্ধান্ত নেয় যে `stop` মান তালিকায় অন্তর্ভুক্ত করা হবে নাকি বাদ দেওয়া হবে। |

### আউটপুট

| নাম | টাইপ | বর্ণনা |
| --- | --- | --- |
| `int` | `INT 𝌠` | int-এ রূপান্তরিত মান (নিচে বৃদ্ধি/ফ্লোর করা)। |
| `float` | `FLOAT 𝌠` | float হিসাবে মান। |
| `string` | `STRING 𝌠` | float হিসাবে মান স্ট্রিং-এ রূপান্তরিত। |
| `index` | `INT 𝌠` | 0..count এর সীমা যা ইনডেক্স হিসাবে ব্যবহার করা যেতে পারে। |
| `count` | `INT` | `num` এর মতোই। |


## Number OutputList

![Number OutputList](NumberOutputList/NumberOutputList.png)

(Вклучен е ComfyUI токов)

Креира OutputList со опсег на броеви.
Внатрешно користи [numpy.linspace](https://numpy.org/doc/stable/reference/generated/numpy.linspace.html), бидејќи работи поефективно со вредности на податоци со покретен запирка.
Ако сакате да дефинирате листа на броеви со произволни чекори, проверете го JSON OutputList и дефинирајте низа, на пр. `[1, 42, 123]`.
`int`, `float`, `string` и `index` користат `is_output_list=True` (означено со симболот `𝌠`) и ќе се обработат секвенцијално од соодветните јазли.

### Влезови

| Име | Тип | Опис |
| --- | --- | --- |
| `start` | `FLOAT` | Почетна вредност за генерирање на опсегот. |
| `stop` | `FLOAT` | Крајна вредност. Ако `endpoint=include` тогаш овој број е вклучен во листата. |
| `num` | `INT` | Бројот на предмети во листата (не го збунувајте со `step`). |
| `endpoint` | `BOOLEAN` | Одлучува дали крајната вредност `stop` треба да биде вклучена или исклучена во предметите. |

### Излезови

| Име | Тип | Опис |
| --- | --- | --- |
| `int` | `INT 𝌠` | Вредноста конвертирана во int (заокружена надолу/подолу). |
| `float` | `FLOAT 𝌠` | Вредноста како float. |
| `string` | `STRING 𝌠` | Вредноста како float конвертирана во низа. |
| `index` | `INT 𝌠` | Опсег од 0..count кој може да се користи како индекс. |
| `count` | `INT` | Иsto како `num`. |


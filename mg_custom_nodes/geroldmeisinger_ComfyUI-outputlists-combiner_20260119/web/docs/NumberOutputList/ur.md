## نمبر آؤٹ پٹ لسٹ

![Number OutputList](NumberOutputList/NumberOutputList.png)

(ComfyUI workflow شامل ہے)

ایک نمبر کی قیمتوں کی رینج کے ساتھ ایک آؤٹ پٹ لسٹ بناتا ہے۔
چونکہ یہ فلوٹنگ پوائنٹ قیمتوں کے ساتھ زیادہ موثوق کام کرتا ہے، اس لیے یہ داخلی طور پر [numpy.linspace](https://numpy.org/doc/stable/reference/generated/numpy.linspace.html) کا استعمال کرتا ہے۔
اگر آپ کو مخصوص اسٹیپس کے ساتھ نمبر کی فہرستیں متعین کرنی ہیں تو JSON OutputList چیک کریں اور ایک ارے کی تعریف کریں، جیسا کہ `[1, 42, 123]`۔
`int`، `float`، `string` اور `index` کا استعمال `is_output_list=True` (علامت `𝌠` کے ذریعے ظاہر ہوتا ہے) کرتا ہے اور متعلقہ نوڈس کے ذریعے ک্রم سے پردازش ہوتا ہے۔

### ان پٹس

| نام | قسم | وضاحت |
| --- | --- | --- |
| `start` | `FLOAT` | رینج کی تیاری کا آغاز کرنے کی قیمت۔ |
| `stop` | `FLOAT` | آخری قیمت۔ اگر `endpoint=include` ہو تو یہ نمبر لسٹ میں شامل ہوتا ہے۔ |
| `num` | `INT` | لسٹ میں آئٹمز کی تعداد (اسے `step` سے ملنا نہیں چاہیے)۔ |
| `endpoint` | `BOOLEAN` | یہ فیصلہ کرتا ہے کہ `stop` قیمت آئٹمز میں شامل ہوگی یا نہیں۔ |

### آؤٹ پٹس

| نام | قسم | وضاحت |
| --- | --- | --- |
| `int` | `INT 𝌠` | قیمت کو int میں تبدیل کرنا (نیچے گئی ہوئی/فلور کی گئی)۔ |
| `float` | `FLOAT 𝌠` | قیمت کے بطور فلوٹ۔ |
| `string` | `STRING 𝌠` | فلوٹ کے طور پر قیمت کو سٹرنگ میں تبدیل کرنا۔ |
| `index` | `INT 𝌠` | 0..count کی رینج جو ایک انڈیکس کے طور پر استعمال کی جا سکتی ہے۔ |
| `count` | `INT` | `num` کی طرح۔ |


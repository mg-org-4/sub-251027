## لیست خروجی صفحه گسترده

![Spreadsheet OutputList](SpreadsheetOutputList/SpreadsheetOutputList.png)

(فایل کارگاه ComfyUI همراه داده شده است)

چندین لیست خروجی را از یک صفحه گسترده (`.csv .tsv .ods .xlsx .xls`) ایجاد می‌کند.
می‌توانید از نود `Load any File` برای بارگذاری یک فایل به رمزگذاری base64 استفاده کنید.
به صورت داخلی از *pandas* [read_excel](https://pandas.pydata.org/pandas-docs/stable/reference/api/pandas.read_excel.html) و [read_csv](https://pandas.pydata.org/pandas-docs/stable/reference/api/pandas.read_csv.html) برای بارگذاری فایل‌های صفحه گسترده استفاده می‌کند.
تمامی لیست‌ها از `is_output_list=True` (که با نماد `𝌠` نشان داده شده است) استفاده می‌کنند و به صورت متوالی توسط نودهای مربوطه پردازش می‌شوند.

### ورودی‌ها

| نام | نوع | توضیح |
| --- | --- | --- |
| `rows_and_cols` | `STRING` | شاخص‌ها و نام‌های ردیف‌ها و ستون‌های صفحه گسترده. توجه داشته باشید که در صفحات گسترده ردیف‌ها از 1 شروع می‌شوند، ستون‌ها از A، در حالی که لیست‌های خروجی از 0 شروع می‌شوند (در `select-nth`). |
| `header_rows` | `INT` | ردیف‌های اول x را در لیست نادیده بگیرید. فقط زمانی استفاده می‌شود که یک ستون در `rows_and_cols` مشخص کنید. |
| `header_cols` | `INT` | ستون‌های اول x را در لیست نادیده بگیرید. فقط زمانی استفاده می‌شود که یک ردیف در `rows_and_cols` مشخص کنید. |
| `select_nth` | `INT` | فقط ورودی nام را انتخاب کنید (شروع از 0). در ترکیب با الگوی `PrimitiveInt+control_after_generate=increment` مفید است. |
| `string_or_base64` | `STRING` | رشته CSV/TSV یا فایل صفحه گسترده به رمزگذاری base64 (برای `.ods .xlsx .xls`). از نود `Load Any File` برای بارگذاری یک فایل به رمزگذاری base64 استفاده کنید. |

### خروجی‌ها

| نام | نوع | توضیح |
| --- | --- | --- |
| `list_a` | `STRING 𝌠` |  |
| `list_b` | `STRING 𝌠` |  |
| `list_c` | `STRING 𝌠` |  |
| `list_d` | `STRING 𝌠` |  |
| `count` | `INT` | تعداد آیتم‌های لیست طولانی‌تر. |


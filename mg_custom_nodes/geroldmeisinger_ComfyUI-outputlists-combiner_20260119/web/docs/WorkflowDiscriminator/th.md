## Workflow Discriminator

![Workflow Discriminator](WorkflowDiscriminator/WorkflowDiscriminator.png)

(ComfyUI workflow ที่รวมอยู่)

เปรียบเทียบ workflows และแยกแยะเพื่อสกัดค่าที่แตกต่างกันเป็น OutputLists แยกต่างหาก
คุณสามารถใช้ node นี้เพื่อเรียกคืนวิธีการสร้างภาพแต่ละภาพจากรายการภาพที่มี workflow เดียวกัน
โปรดทราบว่า `IMAGE` ของ ComfyUI ไม่ประกอบด้วยข้อมูลเมตาของ workflow และคุณต้องโหลดภาพด้วยตัวโหลดภาพ+เมตาดาต้าเฉพาะ และเชื่อมเมตาดาต้าเข้ากับ node นี้
node ที่มีตัวโหลดเมตาดาต้า ได้แก่:
* `Load Any File.metadata` -> `JSON OutputList(jsonpath=$.["PNG:Prompt"]).value`
* [Crystool](https://github.com/crystian/ComfyUI-Crystools) `🪛 Load image with metadata.Metadata RAW` -> `🪛 Metadata extractor.prompt`
* [Simple_Readable_Metadata](https://github.com/ShammiG/ComfyUI-Simple_Readable_Metadata-SG) `Simple Readable Metadata-SG.metadata_raw`

### ป้อนข้อมูล

| ชื่อ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| `objs_0` | `*` | (ไม่บังคับ) ออบเจกต์เดียว (หรือรายการของออบเจกต์) ซึ่งมักจะเป็น workflow `objs_0` และ `more_objs` จะถูกเชื่อมต่อกันและมีอยู่เพื่อความสะดวก ถ้าคุณต้องการเปรียบเทียบเพียงสองออบเจกต์ |
| `more_objs` | `*` | (ไม่บังคับ) ออบเจกต์อื่น (หรือรายการของออบเจกต์) ซึ่งมักจะเป็น workflow `objs_0` และ `more_objs` จะถูกเชื่อมต่อกันและมีอยู่เพื่อความสะดวก ถ้าคุณต้องการเปรียบเทียบเพียงสองออบเจกต์ |
| `ignore_jsonpaths` | `STRING` | (ไม่บังคับ) รายการของ JSONPaths ที่จะละเว้น ในกรณีที่คุณต้องการเชื่อมต่อหลายตัวแยกแยะเข้าด้วยกัน |

### ผลลัพธ์

| ชื่อ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| `list_a` | `* 𝌠` |  |
| `list_b` | `* 𝌠` |  |
| `list_c` | `* 𝌠` |  |
| `list_d` | `* 𝌠` |  |
| `jsonpaths` | `STRING 𝌠` |  |


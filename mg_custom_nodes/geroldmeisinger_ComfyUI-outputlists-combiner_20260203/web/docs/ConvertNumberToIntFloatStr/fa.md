## تبدیل به عدد صحیح، اعشاری و رشته

![Convert To Int Float Str](ConvertNumberToIntFloatStr/ConvertNumberToIntFloatStr.png)

(فایل کارگاه ComfyUI همراه داده شده است)

این نود هر چیزی که شبیه عدد باشد را به `عدد صحیح` `عدد اعشاری` `رشته` تبدیل می‌کند.
از تابع `nums_from_string.get_nums` به صورت داخلی استفاده می‌کند که بسیار انعطاف‌پذیر در پذیرش اعداد است. هر چیزی از عدد صحیح، عدد اعشاری، عدد صحیح یا اعشاری به صورت رشته، رشته‌ای که حاوی چندین عدد با جداکننده هزار است را پذیرفته می‌شود.
از رشته `123;234;345` برای سریع تولید یک لیست اعداد استفاده کنید. از کاما به عنوان جداکننده استفاده نکنید زیرا ممکن است به عنوان جداکننده هزار تفسیر شود.
`int`، `float` و `string` از `is_output_list=True` (که با نماد `𝌠` نشان داده شده است) استفاده می‌کنند و به صورت متوالی توسط نودهای مربوطه پردازش می‌شوند.

### ورودی‌ها

| نام | نوع | توضیح |
| --- | --- | --- |
| `any` | `*` | هر چیزی که بتوان به راحتی به رشته با اعداد قابل تجزیه تبدیل کرد |

### خروجی‌ها

| نام | نوع | توضیح |
| --- | --- | --- |
| `int` | `INT 𝌠` | تمام اعداد پیدا شده در رشته با رقم اعشار حذف شده. |
| `float` | `FLOAT 𝌠` | تمام اعداد پیدا شده در رشته به صورت اعشاری. |
| `string` | `STRING 𝌠` | تمام اعداد پیدا شده در رشته به صورت اعشاری و سپس به رشته تبدیل شده. |
| `count` | `INT` | تعداد اعداد پیدا شده در مقدار. |


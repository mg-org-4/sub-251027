## Number OutputList

![Number OutputList](NumberOutputList/NumberOutputList.png)

(ներառյալ ComfyUI աշխատահետևակայություն)

Ստեղծում է OutputList թվային արժեքների մի շարքով:
Ներքին կերպարան օգտագործում է [numpy.linspace](https://numpy.org/doc/stable/reference/generated/numpy.linspace.html), քանի որ այն ավելի անվավերական է լինում լինում թվային արժեքների հետ:
Եթե ցանկանում եք սահմանել ցանկացած քայլերով թվային ցուցակներ, ապա տեսարանեք JSON OutputList և սահմանեք զանգված, օրինակ `[1, 42, 123]`:
`int`, `float`, `string` և `index` օգտագործում են `is_output_list=True` (նշված է `𝌠` նիշով) և կատարվելու են համապատասխան հանգամանքներում հաջորդական կերպով:

### Մուտքեր

| Անուն | Տիպ | Նկարագրություն |
| --- | --- | --- |
| `start` | `FLOAT` | Սկզբնական արժեքը շարքը ստեղծելու համար: |
| `stop` | `FLOAT` | Ավարտական արժեքը: Եթե `endpoint=include` է, ապա այդ թիվը ներառվում է ցուցակում: |
| `num` | `INT` | Ցուցակի տարրերի քանակը (մի հիշեք այն քայլի հետ առաջացնող արժեքից): |
| `endpoint` | `BOOLEAN` | Որոշում է անում, թե ավարտական արժեքը ներառվի թե արժեքը անտեսվի տարրերում: |

### Ելքեր

| Անուն | Տիպ | Նկարագրություն |
| --- | --- | --- |
| `int` | `INT 𝌠` | Արժեքը ամբողջ թվի տեսքով (կլորացված վերև): |
| `float` | `FLOAT 𝌠` | Արժեքը լինում է թվային տեսքով: |
| `string` | `STRING 𝌠` | Արժեքը թվային տեսքով անձնական տեսքով փոխարինվում է: |
| `index` | `INT 𝌠` | 0..count շարքը, որը կարող է օգտագործվել ինդեքսի տեսքով: |
| `count` | `INT` | Նույնը, ինչ `num`: |


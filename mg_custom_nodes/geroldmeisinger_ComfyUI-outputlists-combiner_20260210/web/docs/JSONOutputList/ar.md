## مخرجات قائمة JSON

![مخرجات قائمة JSON](JSONOutputList/JSONOutputList.png)

(مدمج في ComfyUI workflow)

يُنشئ قائمة مخرجات عن طريق استخراج المصفوفات أو القواميس من كائنات JSON.
يستخدم صيغة JSONPath لاستخراج القيم، انظر [JSONPath على ويكيبيديا](https://en.wikipedia.org/wiki/JSONPath) .
تُدمج جميع القيم المطابقة في قائمة واحدة طويلة.
يمكنك أيضًا استخدام هذه العقدة لإنشاء كائنات من سلاسل نصية مثل `[1, 2, 3]`.
تستخدم `key` و `value` و `int` و `float` `is_output_list=True` (يُشير إليها برمز `𝌠`) وسيتم معالجتها تسلسليًا بواسطة العقد المقابلة.

### المدخلات

| الاسم | النوع | الوصف |
| --- | --- | --- |
| `jsonpath` | `STRING` | JSONPath المستخدم لاستخراج القيم. |
| `json` | `STRING` | سلسلة JSON تُترجم إلى كائن. |
| `obj` | `*` | (اختياري) كائن من أي نوع سيتم استبدال سلسلة JSON |

### المخرجات

| الاسم | النوع | الوصف |
| --- | --- | --- |
| `key` | `STRING 𝌠` | المفتاح للقواميس أو الفهرس للمصفوفات (كسلسلة). فعليًا هو فهرس عالمي للقائمة المدمجة لجميع غير المفاتيح. |
| `value` | `STRING 𝌠` | القيمة كسلسلة. |
| `int` | `INT 𝌠` | القيمة كعدد صحيح (إذا لم تستطع تحليل الرقم، تُستخدم القيمة الافتراضية 0). |
| `float` | `FLOAT 𝌠` | القيمة كعدد عشري (إذا لم تستطع تحليل الرقم، تُستخدم القيمة الافتراضية 0). |
| `count` | `INT` | عدد إجمالي العناصر في القائمة المدمجة |
| `debug` | `STRING` | إخراج التصحيح لجميع الكائنات المطابقة كسلسلة JSON منسقة |


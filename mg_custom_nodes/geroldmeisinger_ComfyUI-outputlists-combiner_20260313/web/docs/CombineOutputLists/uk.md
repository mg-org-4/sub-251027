## Комбінації OutputLists

![Комбінації OutputLists](CombineOutputLists/CombineOutputLists.png)

(Включено ComfyUI workflow)

Приймає до 4 OutputLists і генерує усі їхні комбінації.

Приклад: `[1, 2, 3] x ["A", "B"] = [(1, "A"), (1, "B"), (2, "A"), (2, "B"), (3, "A"), (3, "B")]`

`unzip_a` .. `unzip_d` використовують `is_output_list=True` (позначено символом `𝌠`) і будуть оброблені послідовно відповідними нодами.

Усі списки є необов’язковими, порожні списки будуть ігноруватися.

Технічно він обчислює *декартовий добуток* і виводить кожну комбінацію, розподілену на її елементи (`unzip`), тоді як порожні списки будуть замінені одиницями `None`, які будуть виводити `None` на відповідних вихідних портах.

Приклад: `[1, 2] x [] x ["A", "B"] x [] = [(1, None, "A", None), (1, None, "B", None), (2, None, "A", None), (2, None, "B", None)]`

### Входи

| Назва | Тип | Опис |
| --- | --- | --- |
| `list_a` | `*` | (необов’язково) |
| `list_b` | `*` | (необов’язково) |
| `list_c` | `*` | (необов’язково) |
| `list_d` | `*` | (необов’язково) |

### Виходи

| Назва | Тип | Опис |
| --- | --- | --- |
| `unzip_a` | `* 𝌠` | Значення комбінацій, що відповідають `list_a`. |
| `unzip_b` | `* 𝌠` | Значення комбінацій, що відповідають `list_b`. |
| `unzip_c` | `* 𝌠` | Значення комбінацій, що відповідають `list_c`. |
| `unzip_d` | `* 𝌠` | Значення комбінацій, що відповідають `list_d`. |
| `index` | `INT 𝌠` | Діапазон від 0..count, який можна використовувати як індекс. |
| `count` | `INT` | Загальна кількість комбінацій. |


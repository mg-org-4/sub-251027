## مخرجات قائمة جدول البيانات

![مخرجات قائمة جدول البيانات](SpreadsheetOutputList/SpreadsheetOutputList.png)

(مدمج في ComfyUI workflow)

يُنشئ عدة قوائم مخرجات من جدول بيانات (`.csv .tsv .ods .xlsx .xls`).
يمكنك استخدام عقدة `Load any File` لتحميل ملف بصيغة base64-encoding.
يستخدم داخليًا *pandas* [read_excel](https://pandas.pydata.org/pandas-docs/stable/reference/api/pandas.read_excel.html) و [read_csv](https://pandas.pydata.org/pandas-docs/stable/reference/api/pandas.read_csv.html) لتحميل ملفات جداول البيانات.
تستخدم جميع القوائم `is_output_list=True` (يُشير إليها برمز `𝌠`) وسيتم معالجتها تسلسليًا بواسطة العقد المقابلة.

### المدخلات

| الاسم | النوع | الوصف |
| --- | --- | --- |
| `rows_and_cols` | `STRING` | مؤشرات وأسماء الصفوف والأعمدة في جدول البيانات. لاحظ أن في جداول البيانات تبدأ الصفوف من 1، والأعمدة تبدأ من A، بينما قوائم المخرجات تعتمد على 0 (في `select-nth`). |
| `header_rows` | `INT` | تجاهل أول x صفوف في القائمة. يُستخدم فقط إذا حددت عمودًا في `rows_and_cols`. |
| `header_cols` | `INT` | تجاهل أول x أعمدة في القائمة. يُستخدم فقط إذا حددت صفًا في `rows_and_cols`. |
| `select_nth` | `INT` | تحديد فقط الإدخال nth (يعتمد على 0). مفيد في التجميع مع نمط `PrimitiveInt+control_after_generate=increment`. |
| `string_or_base64` | `STRING` | سلسلة CSV/TSV أو ملف جدول بيانات بصيغة base64 (لـ `.ods .xlsx .xls`). استخدم عقدة `Load Any File` لتحميل ملف بصيغة base64. |

### المخرجات

| الاسم | النوع | الوصف |
| --- | --- | --- |
| `list_a` | `STRING 𝌠` |  |
| `list_b` | `STRING 𝌠` |  |
| `list_c` | `STRING 𝌠` |  |
| `list_d` | `STRING 𝌠` |  |
| `count` | `INT` | عدد العناصر في أطول قائمة. |


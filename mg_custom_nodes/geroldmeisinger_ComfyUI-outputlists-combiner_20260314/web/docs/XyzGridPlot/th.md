## XYZ-GridPlot

![XYZ-GridPlot](XyzGridPlot/XyzGridPlot.png)

(ComfyUI workflow รวมอยู่ในไฟล์)

สร้าง XYZ-Gridplot จากรายการภาพ
มันจะรับรายการภาพ (รวมถึง batches) และแปลงให้เป็นรายการยาว (ดังนั้น `batch_size=1`)

**รูปร่างของกริด**
กำหนดรูปร่างของกริดโดย:
1. จำนวนของชื่อแถว
2. จำนวนของชื่อคอลัมน์
3. จำนวนของภาพย่อยที่เหลือ
คุณสามารถใช้ `order=inside_out` เพื่อเรียงลำดับภาพแบบย้อนกลับ (มีประโยชน์หาก `batch_size>1` และคุณต้องการติดป้ายชื่อของ batch)

**การจัดตำแหน่ง**
* หากชื่อถูกตัดบรรทัดลงมา ทั้งแกนจะถูกพิจารณาว่าเป็น "หลายบรรทัด" และจัดวางให้อยู่บนสุดพร้อมกับการกระจายระยะห่าง
* หากชื่อทั้งหมดเป็นตัวเลขหรือสิ้นสุดด้วยตัวเลข (เช่น `strength: 1.`) ทั้งแกนจะถูกพิจารณาว่าเป็น "ตัวเลข" และจัดวางให้อยู่ทางขวา
* ข้อความอื่น ๆ จะถูกพิจารณาว่าเป็น "หนึ่งบรรทัด" และจัดวางให้อยู่ตรงกลาง
* จัดตำแหน่งชื่อที่เป็นหนึ่งบรรทัดและตัวเลขของคอลัมน์ไว้ด้านล่าง และจัดวางชื่อของแถวไว้ตรงกลางแนวตั้ง

**ขนาดตัวอักษร**
* ความสูงของพื้นที่ชื่อคอลัมน์จะถูกกำหนดโดย `font_size` หรือ `ครึ่งหนึ่งของความสูงของการจัดเรียงภาพย่อยที่มากที่สุดในแต่ละแถว` (ขึ้นอยู่กับค่าที่มากกว่า)
* ความกว้างของพื้นที่ชื่อแถวจะถูกกำหนดโดยความกว้างที่สุดของการจัดเรียงภาพย่อย (อย่างน้อย 256px)
* ข้อความจะถูกย่อให้เข้าพอดี (ถึง `font_size_min=6`) และใช้ขนาดตัวอักษรเดียวกันสำหรับทั้งแกน (ชื่อแถวหรือชื่อคอลัมน์)
หากขนาดตัวอักษรอยู่ที่ต่ำสุดแล้ว จะตัดข้อความที่เหลือ

**การจัดเรียงภาพย่อย**
จัดรูปร่างของภาพย่อย (โดยทั่วไปมาจาก batches) ให้อยู่ในพื้นที่ที่เป็นสี่เหลี่ยมที่สุด (การ "จัดเรียงภาพย่อย") เว้นแต่ `output_is_list=True` ซึ่งจะใช้ภาพเดียวสำหรับแต่ละเซลล์และสร้างรายการของกริดภาพเต็มแทน
คุณสามารถใช้รายการของกริดภาพนี้เพื่อเชื่อมต่อกับโหนด XyzGridPlot อื่นเพื่อสร้าง super-grids
หากภาพย่อยประกอบด้วย batches ที่มีขนาดต่างกัน จะเติมเซลล์ที่ขาดหายไปด้วยภาพว่าง
จำนวนภาพต่อเซลล์ (รวมถึงภาพจาก batch) ต้องเป็นผลคูณของ `rows * columns`

### ป้อนข้อมูล

| ชื่อ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| `images` | `IMAGE` | รายการภาพ (รวมถึง batches) |
| `row_labels` | `*` | ข้อความชื่อแถวที่อยู่ด้านซ้าย |
| `col_labels` | `*` | ข้อความชื่อคอลัมน์ที่อยู่ด้านบน |
| `gap` | `INT` | ช่องว่างระหว่างการจัดเรียงภาพย่อย หมายเหตุว่าภายในภาพย่อยจะไม่มีช่องว่าง หากคุณต้องการช่องว่างระหว่างภาพย่อย ให้เชื่อมต่อกับโหนด XyzGridPlot อื่น |
| `font_size` | `FLOAT` | ขนาดตัวอักษรเป้าหมาย ข้อความจะถูกย่อให้เข้าพอดี (ถึง `font_size_min=6`) |
| `row_label_orientation` | `COMBO` | ทิศทางของข้อความชื่อแถว มีประโยชน์หากคุณต้องการประหยัดพื้นที่ |
| `order` | `BOOLEAN` | กำหนดลำดับการประมวลผลภาพ นี่จะมีความสำคัญเฉพาะเมื่อคุณมีภาพย่อย มีประโยชน์หาก `batch_size>1` และคุณต้องการวาดกริดของ batch |
| `output_is_list` | `BOOLEAN` | นี่จะมีความสำคัญเฉพาะเมื่อคุณมีภาพย่อยหรือต้องการสร้าง super-grids |

### ผลลัพธ์

| ชื่อ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| `image` | `IMAGE 𝌠` | ภาพ XYZ-GridPlot หาก `output_is_list=True` จะสร้างรายการภาพที่คุณสามารถเชื่อมต่อกับโหนด XYZ-GridPlot อื่นเพื่อสร้าง super-grids |


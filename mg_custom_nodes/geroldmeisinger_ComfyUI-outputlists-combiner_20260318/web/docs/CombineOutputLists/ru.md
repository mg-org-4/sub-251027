## Комбинации OutputLists

![Комбинации OutputLists](CombineOutputLists/CombineOutputLists.png)

(Включена ComfyUI workflow)

Принимает до 4 OutputLists и генерирует все их комбинации.

Пример: `[1, 2, 3] x ["A", "B"] = [(1, "A"), (1, "B"), (2, "A"), (2, "B"), (3, "A"), (3, "B")]`

`unzip_a` .. `unzip_d` используют `is_output_list=True` (обозначено символом `𝌠`) и будут обработаны последовательно соответствующими узлами.

Все списки являются необязательными, и пустые списки будут проигнорированы.

Технически он вычисляет *декартово произведение* и выводит каждую комбинацию, разбитую на элементы (`unzip`), при этом пустые списки будут заменены единицами `None`, которые будут выдавать `None` на соответствующих выходах.

Пример: `[1, 2] x [] x ["A", "B"] x [] = [(1, None, "A", None), (1, None, "B", None), (2, None, "A", None), (2, None, "B", None)]`

### Входы

| Имя | Тип | Описание |
| --- | --- | --- |
| `list_a` | `*` | (необязательно) |
| `list_b` | `*` | (необязательно) |
| `list_c` | `*` | (необязательно) |
| `list_d` | `*` | (необязательно) |

### Выходы

| Имя | Тип | Описание |
| --- | --- | --- |
| `unzip_a` | `* 𝌠` | Значение комбинаций, соответствующих `list_a`. |
| `unzip_b` | `* 𝌠` | Значение комбинаций, соответствующих `list_b`. |
| `unzip_c` | `* 𝌠` | Значение комбинаций, соответствующих `list_c`. |
| `unzip_d` | `* 𝌠` | Значение комбинаций, соответствующих `list_d`. |
| `index` | `INT 𝌠` | Диапазон от 0 до count, который может использоваться как индекс. |
| `count` | `INT` | Общее количество комбинаций. |


## XYZ-GridPlot

![XYZ-GridPlot](XyzGridPlot/XyzGridPlot.png)

(ComfyUI workflow مرفق)

يُنشئ رسمًا XYZ-Gridplot من قائمة من الصور.
يأخذ قائمة من الصور (بما في ذلك الدُفعات) ويرتبها في قائمة طويلة أولاً (وبالتالي `batch_size=1`).

**شكل الشبكة**
يحدد شكل الشبكة من خلال:
1. عدد تسميات الصفوف
2. عدد تسميات الأعمدة
3. عدد الصور الفرعية المتبقية.
يمكنك استخدام `order=inside_out` لعكس اختيار الصور (مفيد إذا كان `batch_size>1` وترغب في تسمية الدُفعات).

**المحاذاة**
* إذا تم تفقيم تسمية إلى السطر التالي، تُعتبر المحور بأكمله "متعدد الأسطر" ويتقاطع مع مسافات متماثلة.
* إذا كانت جميع التسميات أرقامًا أو جميعها تنتهي بأرقام (مثل `strength: 1.`) تُعتبر المحور بأكمله "رقميًا" ويتم محاذاة التسميات إلى اليمين.
* جميع النصوص الأخرى تُعتبر "أحادية السطر" وتحتاج إلى محاذاة مركزية.
* تُحاذي التسميات الأحادية والرقمية للأعمدة في الأسفل، وتحتاج إلى محاذاة الأعمدة في المنتصف عموديًا.

**حجم الخط**
* يُحدد ارتفاع منطقة تسمية الأعمدة بواسطة `font_size` أو `نصف أعلى ارتفاع تعبئة الصور الفرعية في أي صف` (بما في ذلك أكبر).
* يُحدد عرض منطقة تسمية الصفوف بواسطة العرض الأكبر للصور الفرعية (بحد أدنى 256px).
* يُقلّص النص حتى يناسب (إلى `font_size_min=6`) ويستخدم نفس حجم الخط لكل المحور (تسميات الصفوف أو الأعمدة).
إذا كان حجم الخط بالفعل في الحد الأدنى، يتم قص أي نص متبقي.

**تعبئة الصور الفرعية**
يُشكّل الصور الفرعية (عادةً من الدُفعات) في أصغر مساحة ممكنة (الـ "تعبئة الصور الفرعية")، إلا إذا كانت `output_is_list=True`، في هذه الحالة تُستخدم صورة واحدة لكل خلية وتُنشئ قائمة من شبكات الصور الكاملة بدلاً من ذلك.
يمكنك استخدام هذه القائمة من شبكات الصور لتوصيل عقدة XyzGridPlot أخرى لإنشاء شبكات فائقة.
إذا كانت الصور الفرعية تتكون من دُفعات بحجم مختلف، تُملأ الخلايا المفقودة بالصور الفارغة.
يجب أن يكون عدد الصور لكل خلية (بما في ذلك الصور المجمعة) مضاعفًا لـ `الصفوف * الأعمدة`.

### المدخلات

| الاسم | النوع | الوصف |
| --- | --- | --- |
| `images` | `IMAGE` | قائمة من الصور (بما في ذلك الدُفعات) |
| `row_labels` | `*` | نصوص تسمية الصفوف على الجانب الأيسر |
| `col_labels` | `*` | نصوص تسمية الأعمدة في الأعلى |
| `gap` | `INT` | الفجوة بين تعبئة الصور الفرعية. لاحظ أن داخل الصور الفرعية لا توجد فجوة. إذا كنت تريد فجوة بين الصور الفرعية، اربط عقدة XyzGridPlot أخرى. |
| `font_size` | `FLOAT` | حجم الخط المستهدف. سيتم تقليل النص حتى يناسب (إلى `font_size_min=6`). |
| `row_label_orientation` | `COMBO` | اتجاه نص تسمية الصفوف. مفيد إذا كنت ترغب في توفير المساحة. |
| `order` | `BOOLEAN` | يحدد ترتيب معالجة الصور. هذا مفيد فقط إذا كانت لديك صور فرعية. مفيد إذا كان `batch_size>1` وترغب في رسم الدُفعات. |
| `output_is_list` | `BOOLEAN` | هذا مفيد فقط إذا كانت لديك صور فرعية أو إذا كنت ترغب في إنشاء شبكات فائقة. |

### المخرجات

| الاسم | النوع | الوصف |
| --- | --- | --- |
| `image` | `IMAGE 𝌠` | صورة XYZ-GridPlot. إذا كانت `output_is_list=True`، فستُنشئ قائمة من الصور يمكنك ربطها بعقدة XYZ-GridPlot أخرى لإنشاء شبكات فائقة. |


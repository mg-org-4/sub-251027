## نمودار XYZ-GridPlot

![XYZ-GridPlot](XyzGridPlot/XyzGridPlot.png)

(فایل کارگاه ComfyUI همراه داده شده است)

از یک لیست از تصاویر یک نمودار XYZ-Gridplot تولید می‌کند.
اول لیستی از تصاویر (شامل دسته‌ها) را گرفته و آنها را ابتدا به یک لیست بلند تبدیل می‌کند (بنابراین `batch_size=1`).

**شکل شبکه**
شکل شبکه را بر اساس:
1. تعداد برچسب‌های ردیف
2. تعداد برچسب‌های ستون
3. زیر تصاویر باقیمانده تعیین می‌کند.
می‌توانید از `order=inside_out` برای معکوس کردن انتخاب تصویر استفاده کنید (مفید است اگر `batch_size>1` و می‌خواهید دسته‌ها را برچسب‌گذاری کنید).

**ترازبندی**
* اگر یک برچسب در خط بعدی پیوند شود، کل محور به عنوان "چندخطی" در نظر گرفته می‌شود و آنها را در بالا با فاصله توجیه‌شده تراز می‌کند.
* اگر تمام برچسب‌ها اعداد باشند یا همگی با اعداد خاتمه یابند (مثلاً `strength: 1.`) کل محور به عنوان "عددی" در نظر گرفته می‌شود و آنها را در راست تراز می‌کند.
* متن‌های دیگر به عنوان "تک‌خطی" در نظر گرفته می‌شوند و آنها را در وسط تراز می‌کند.
* برچسب‌های تک‌خطی و عددی را برای ستون‌ها در پایین و برای ردیف‌ها عمودی در وسط تراز می‌کند.

**اندازه قلم**
* ارتفاع ناحیه برچسب ستون بر اساس `font_size` یا `نصف بیشترین ارتفاع بسته‌بندی زیر تصاویر در هر ردیف` (هر کدام بیشتر باشد) تعیین می‌شود.
* عرض ناحیه برچسب ردیف بر اساس عرض گسترده‌ترین بسته‌بندی زیر تصاویر (حداقل 256px) تعیین می‌شود.
* متن تا زمانی که جا بگیرد کوچک می‌شود (تا `font_size_min=6`) و از همان اندازه قلم برای کل محور (برچسب‌های ردیف یا ستون) استفاده می‌کند.
اگر اندازه قلم در حداقل باشد، هر متن باقیمانده را برش می‌دهد.

**بسته‌بندی زیر تصاویر**
زیر تصاویر (معمولاً از دسته‌ها) را به بیشترین مربع مناسب («بسته‌بندی زیر تصاویر») شکل می‌دهد، مگر اینکه `output_is_list=True` باشد، در این صورت فقط یک تصویر برای هر سلول استفاده کرده و لیستی از شبکه‌های تصویر کامل ایجاد می‌کند.
می‌توانید از این لیست شبکه‌های تصویری برای متصل کردن یک نود XyzGridPlot دیگر برای ایجاد سوپر-شبکه‌ها استفاده کنید.
اگر زیر تصاویر از دسته‌هایی با اندازه‌های مختلف تشکیل شده باشند، سلول‌های کمبود را با تصاویر خالی پر می‌کند.
تعداد تصاویر در هر سلول (شامل تصاویر دسته‌ای) باید مضربی از `rows * columns` باشد.

### ورودی‌ها

| نام | نوع | توضیح |
| --- | --- | --- |
| `images` | `IMAGE` | لیستی از تصاویر (شامل دسته‌ها) |
| `row_labels` | `*` | متن‌های برچسب ردیف در سمت چپ |
| `col_labels` | `*` | متن‌های برچسب ستون در بالا |
| `gap` | `INT` | فاصله بین بسته‌بندی‌های زیر تصویر. توجه داشته باشید که درون خود زیر تصاویر فاصله‌ای استفاده نمی‌شود. اگر می‌خواهید فاصله‌ای بین زیر تصاویر داشته باشید یک نود XyzGridPlot دیگر را متصل کنید. |
| `font_size` | `FLOAT` | اندازه قلم هدف. متن تا زمانی که جا بگیرد کوچک می‌شود (تا `font_size_min=6`). |
| `row_label_orientation` | `COMBO` | جهت متن برچسب‌های ردیف. مفید است اگر می‌خواهید فضای کمتری ذخیره کنید. |
| `order` | `BOOLEAN` | تعریف می‌کند که تصاویر در چه ترتیبی پردازش شوند. این فقط در صورتی مرتبط است که زیر تصاویر داشته باشید. مفید است اگر `batch_size>1` و می‌خواهید دسته‌ها را رسم کنید. |
| `output_is_list` | `BOOLEAN` | این فقط در صورتی مرتبط است که زیر تصاویر داشته باشید یا می‌خواهید سوپر-شبکه‌ها ایجاد کنید. |

### خروجی‌ها

| نام | نوع | توضیح |
| --- | --- | --- |
| `image` | `IMAGE 𝌠` | تصویر XYZ-GridPlot. اگر `output_is_list=True` لیستی از تصاویر ایجاد می‌کند که می‌توانید آنها را به یک نود XYZ-GridPlot دیگر متصل کنید تا سوپر-شبکه‌ها ایجاد کنید. |


## XYZ-GridPlot

![XYZ-GridPlot](XyzGridPlot/XyzGridPlot.png)

(ComfyUI workflow включен)

Генерирует XYZ-Gridplot из списка изображений.
Он принимает список изображений (включая пакеты) и сначала преобразует их в длинный список (поэтому `batch_size=1`).

**Форма сетки**
Определяет форму сетки по:
1. количеству меток строк
2. количеству меток столбцов
3. оставшимся под-изображениям.
Вы можете использовать `order=inside_out`, чтобы изменить порядок выбора изображений (полезно, если `batch_size>1` и вы хотите пометить пакеты).

**Выравнивание**
* Если метка переносится на следующую строку, вся ось считается "многострочной" и выравнивается по верху с выравниванием по ширине.
* Если все метки являются числами или заканчиваются на числа (например, `strength: 1.`), вся ось считается "числовой" и выравнивается по правому краю.
* Все остальные тексты считаются "однострочными" и выравниваются по центру.
* Однострочные и числовые метки для столбцов выравниваются по низу, а для строк — по вертикальному центру.

**Размер шрифта**
* Высота области меток столбцов определяется значением `font_size` или `половина наибольшей высоты упаковки под-изображений в любой строке` (в зависимости от того, что больше).
* Ширина области меток строк определяется максимальной шириной упаковки под-изображений (с минимальным значением 256px).
* Текст сжимается до тех пор, пока он не поместится (до `font_size_min=6`) и использует одинаковый размер шрифта для всей оси (метки строк или столбцов).
Если размер шрифта уже минимален, обрезает оставшийся текст.

**Упаковка под-изображений**
Формирует под-изображения (обычно из пакетов) в наиболее квадратную область («упаковка под-изображений»), если только `output_is_list=True`, в этом случае использует только одно изображение для каждой ячейки и создает список полных сеток изображений.
Вы можете использовать этот список сеток изображений, чтобы подключить другой узел XyzGridPlot и создать супер-сетки.
Если под-изображения состоят из пакетов разного размера, заполняет недостающие ячейки пустыми изображениями.
Количество изображений в ячейках (включая пакетные изображения) должно быть кратно `rows * columns`.

### Входы

| Имя | Тип | Описание |
| --- | --- | --- |
| `images` | `IMAGE` | Список изображений (включая пакеты) |
| `row_labels` | `*` | Тексты меток строк слева |
| `col_labels` | `*` | Тексты меток столбцов сверху |
| `gap` | `INT` | Расстояние между упаковками под-изображений. Обратите внимание, что внутри самих под-изображений расстояние не используется. Если вы хотите расстояние между под-изображениями, подключите другой узел XyzGridPlot. |
| `font_size` | `FLOAT` | Целевой размер шрифта. Текст будет сжат до тех пор, пока он не поместится (до `font_size_min=6`). |
| `row_label_orientation` | `COMBO` | Ориентация текста меток строк. Полезно, если вы хотите сэкономить место. |
| `order` | `BOOLEAN` | Определяет порядок обработки изображений. Это имеет значение только если у вас есть под-изображения. Полезно, если `batch_size>1` и вы хотите отобразить пакеты. |
| `output_is_list` | `BOOLEAN` | Это имеет значение только если у вас есть под-изображения или вы хотите создать супер-сетки. |

### Выходы

| Имя | Тип | Описание |
| --- | --- | --- |
| `image` | `IMAGE 𝌠` | Изображение XYZ-GridPlot. Если `output_is_list=True`, создает список изображений, который вы можете подключить к другому узлу XYZ-GridPlot для создания супер-сеток. |


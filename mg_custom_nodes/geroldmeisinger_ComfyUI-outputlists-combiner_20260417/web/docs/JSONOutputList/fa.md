## خروجی لیست JSON

![JSON OutputList](JSONOutputList/JSONOutputList.png)

(فایل کارگاه ComfyUI همراه داده شده است)

این نود یک لیست خروجی ایجاد می‌کند با استخراج آرایه‌ها یا دیکشنری‌ها از اشیاء JSON.
از سینتکس JSONPath برای استخراج مقادیر استفاده می‌کند، برای اطلاعات بیشتر به [JSONPath در ویکی‌پدیا](https://en.wikipedia.org/wiki/JSONPath) مراجعه کنید.
تمام مقادیر مطابق یافته به یک لیست طولانی تبدیل می‌شوند.
همچنین می‌توانید از این نود برای ایجاد اشیاء از رشته‌های متنی مانند `[1, 2, 3]` استفاده کنید.
`key`، `value`، `int` و `float` از `is_output_list=True` (که با نماد `𝌠` نشان داده شده است) استفاده می‌کنند و به صورت متوالی توسط نودهای مربوطه پردازش می‌شوند.

### ورودی‌ها

| نام | نوع | توضیح |
| --- | --- | --- |
| `jsonpath` | `STRING` | JSONPath مورد استفاده برای استخراج مقادیر. |
| `json` | `STRING` | یک رشته JSON که به یک شیء ترجمه می‌شود. |
| `obj` | `*` | (اختیاری) شیء هر نوعی که جای رشته JSON قرار می‌گیرد |

### خروجی‌ها

| نام | نوع | توضیح |
| --- | --- | --- |
| `key` | `STRING 𝌠` | کلید برای دیکشنری‌ها یا شاخص برای آرایه‌ها (به صورت رشته). به صورت فنی شاخص جهانی لیست تخت شده برای تمام غیرکلیدها است. |
| `value` | `STRING 𝌠` | مقدار به صورت رشته. |
| `int` | `INT 𝌠` | مقدار به صورت عدد صحیح (اگر نتواند عدد را تجزیه کند، به صورت پیش‌فرض 0 است). |
| `float` | `FLOAT 𝌠` | مقدار به صورت عدد اعشاری (اگر نتواند عدد را تجزیه کند، به صورت پیش‌فرض 0 است). |
| `count` | `INT` | تعداد کل آیتم‌ها در لیست تخت شده |
| `debug` | `STRING` | خروجی اشکال‌زدایی تمام اشیاء مطابق یافته به صورت رشته JSON قالب‌بندی شده |


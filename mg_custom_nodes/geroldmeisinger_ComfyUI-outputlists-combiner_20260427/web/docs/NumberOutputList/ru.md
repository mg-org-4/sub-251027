## Number OutputList

![Number OutputList](NumberOutputList/NumberOutputList.png)

(ComfyUI workflow включен)

Создает OutputList с диапазоном числовых значений.
Внутри использует [numpy.linspace](https://numpy.org/doc/stable/reference/generated/numpy.linspace.html), потому что он работает более надежно с вещественными значениями.
Если вы хотите определить списки чисел с произвольным шагом, ознакомьтесь с JSON OutputList и определите массив, например `[1, 42, 123]`.
`int`, `float`, `string` и `index` используют `is_output_list=True` (обозначено символом `𝌠`) и будут обрабатываться последовательно соответствующими нодами.

### Входы

| Имя | Тип | Описание |
| --- | --- | --- |
| `start` | `FLOAT` | Начальное значение для генерации диапазона. |
| `stop` | `FLOAT` | Конечное значение. Если `endpoint=include`, то это число будет включено в список. |
| `num` | `INT` | Количество элементов в списке (не путать с `step`). |
| `endpoint` | `BOOLEAN` | Определяет, должно ли значение `stop` быть включено или исключено из элементов. |

### Выходы

| Имя | Тип | Описание |
| --- | --- | --- |
| `int` | `INT 𝌠` | Значение, преобразованное в int (округлено вниз/принято). |
| `float` | `FLOAT 𝌠` | Значение как float. |
| `string` | `STRING 𝌠` | Значение как float, преобразованное в строку. |
| `index` | `INT 𝌠` | Диапазон от 0..count, который можно использовать как индекс. |
| `count` | `INT` | То же самое, что и `num`.


## OutputLists 組合

![OutputLists Combinations](CombineOutputLists/CombineOutputLists.png)

(包含 ComfyUI workflow)

最多接受 4 個 OutputLists 並產生它們的所有組合。

範例：`[1, 2, 3] x ["A", "B"] = [(1, "A"), (1, "B"), (2, "A"), (2, "B"), (3, "A"), (3, "B")]`

`unzip_a` .. `unzip_d` 使用 `is_output_list=True` (以符號 `𝌠` 表示)，並將由對應的節點依序處理。

所有列表皆為可選，空列表將被忽略。

從技術上來說，它計算的是 *笛卡爾積*，並輸出每個組合分割後的元素（`unzip`），而空列表將被替換為 `None`，並在相應的輸出上發送 `None`。

範例：`[1, 2] x [] x ["A", "B"] x [] = [(1, None, "A", None), (1, None, "B", None), (2, None, "A", None), (2, None, "B", None)]`

### 輸入

| 名稱 | 類型 | 描述 |
| --- | --- | --- |
| `list_a` | `*` | (可選) |
| `list_b` | `*` | (可選) |
| `list_c` | `*` | (可選) |
| `list_d` | `*` | (可選) |

### 輸出

| 名稱 | 類型 | 描述 |
| --- | --- | --- |
| `unzip_a` | `* 𝌠` | 與 `list_a` 對應的組合值。 |
| `unzip_b` | `* 𝌠` | 與 `list_b` 對應的組合值。 |
| `unzip_c` | `* 𝌠` | 與 `list_c` 對應的組合值。 |
| `unzip_d` | `* 𝌠` | 與 `list_d` 對應的組合值。 |
| `index` | `INT 𝌠` | 範圍為 0..count，可用作索引。 |
| `count` | `INT` | 總組合數。


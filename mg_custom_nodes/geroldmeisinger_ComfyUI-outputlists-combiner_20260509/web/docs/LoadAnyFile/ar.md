## تحميل أي ملف

![تحميل أي ملف](LoadAnyFile/LoadAnyFile.png)

(مدمج في ComfyUI workflow)

يحمّل أي ملف نصي أو ثنائي ويقدم محتوى الملف كسلسلة أو كسلسلة base64. كما يحاول تحميله ك`IMAGE`. ويلزم أيضًا تحميل أي بيانات وصفية.

يدعم `filepath` مسارات الملفات المُعلّنة في ComfyUI `[input]` `[output]` أو `[temp]`.
يدعم `filepath` أيضًا توسيعات أنماط glob `subdir/**/*.png`.
يستخدم داخليًا python's [glob.iglob](https://docs.python.org/3/library/glob.html#glob.iglob).

يقوم `metadata` بـ `exiftool`، إذا كان مثبتًا ومتوفرًا في `PATH`، وإلا يستخدم `PIL.Image.info` كبدائل.

لأسباب أمنية، تُدعم فقط الدلائل التالية: `[input] [output] [temp]`.
لأسباب أداء، يُحدّد عدد الملفات إلى: 1024.

### المدخلات

| الاسم | النوع | الوصف |
| --- | --- | --- |
| `filepath` | `STRING` | الدليل الأساسي يُفترض `[input]` الدليل المستخدم. يدعم توسيع أنماط glob `subdir/**/*.png`. استخدم اللاحقة ` [input]` ` [output]` أو ` [temp]` (انتبه للمسافة القيادية!) لتحديد دليل مستخدم ComfyUI مختلف. |

### المخرجات

| الاسم | النوع | الوصف |
| --- | --- | --- |
| `content` | `STRING 𝌠` | محتوى الملف للملفات النصية، base64 للملفات الثنائية. |
| `image` | `IMAGE 𝌠` | مصفوفة دُفعات الصورة. |
| `mask` | `MASK 𝌠` | مصفوفة دُفعات القناع. |
| `metadata` | `STRING 𝌠` | بيانات Exif من ExifTool. يتطلب أن يكون الأمر `exiftool` متوفرًا في `PATH`. |


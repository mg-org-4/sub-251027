## XYZ-GridPlot

![XYZ-GridPlot](XyzGridPlot/XyzGridPlot.png)

(Включено ComfyUI workflow)

Створює XYZ-Gridplot зі списку зображень.
Він приймає список зображень (включаючи пакети) і спочатку розгортає їх у довгий список (тому `batch_size=1`).

**Форма сітки**
Визначає форму сітки за:
1. кількістю міток рядків
2. кількістю міток стовпців
3. залишковими підзображеннями.
Ви можете використовувати `order=inside_out` для зворотного вибору зображень (корисно, якщо `batch_size>1` і ви хочете позначити пакети).

**Вирівнювання**
* Якщо мітка переноситься на наступний рядок, увесь вісь вважається "багаторядковим" і вирівнюється зверху з вирівнюванням за шириною.
* Якщо всі мітки є числами або всі закінчуються числами (наприклад, `strength: 1.`), увесь вісь вважається "числовим" і вирівнюється праворуч.
* Весь інший текст вважається "однорядковим" і вирівнюється по центру.
* Вирівнює однорядкові та числові мітки для стовпців знизу, а для рядків вирівнює їх вертикально посередині.

**Розмір шрифту**
* Висота області міток стовпців визначається `font_size` або `половина найбільшої висоти упаковки підзображень у будь-якому рядку` (залежно від того, що більше).
* Ширина області міток рядків визначається найширшою шириною упаковки підзображень (з мінімумом 256px).
* Текст зменшується до тих пір, поки не поміститься (до `font_size_min=6`) і використовує один і той самий розмір шрифту для всього вісь (мітки рядків або мітки стовпців).
Якщо розмір шрифту вже досягнув мінімального, обрізає будь-який залишковий текст.

**Упаковка підзображень**
Формує підзображення (зазвичай з пакетів) у найбільш квадратну область («упаковка підзображень»), якщо `output_is_list=True`, у цьому випадку використовує лише одне зображення для кожної комірки і створює список повних зображень сіток замість цього.
Ви можете використовувати цей список зображень сіток, щоб підключити інший вузол XyzGridPlot і створити супер-сітки.
Якщо підзображення складаються з пакетів різних розмірів, заповнює відсутні комірки порожніми зображеннями.
Кількість зображень на комірки (включаючи пакетні зображення) має бути кратною `rows * columns`.

### Вхідні дані

| Назва | Тип | Опис |
| --- | --- | --- |
| `images` | `IMAGE` | Список зображень (включаючи пакети) |
| `row_labels` | `*` | Тексти міток рядків зліва |
| `col_labels` | `*` | Тексти міток стовпців зверху |
| `gap` | `INT` | Відстань між упаковками підзображень. Зауважте, що всередині самих підзображень використовується без відстані. Якщо ви хочете відстань між підзображеннями, підключіть інший вузол XyzGridPlot. |
| `font_size` | `FLOAT` | Цільовий розмір шрифту. Текст буде зменшено до тих пір, поки не поміститься (до `font_size_min=6`). |
| `row_label_orientation` | `COMBO` | Орієнтація тексту міток рядків. Корисно, якщо ви хочете заощадити місце. |
| `order` | `BOOLEAN` | Визначає порядок обробки зображень. Це має значення лише у випадку, якщо у вас є підзображення. Корисно, якщо `batch_size>1` і ви хочете відобразити пакети. |
| `output_is_list` | `BOOLEAN` | Це має значення лише у випадку, якщо у вас є підзображення або ви хочете створити супер-сітки. |

### Вихідні дані

| Назва | Тип | Опис |
| --- | --- | --- |
| `image` | `IMAGE 𝌠` | Зображення XYZ-GridPlot. Якщо `output_is_list=True`, створює список зображень, який ви можете підключити до іншого вузла XYZ-GridPlot, щоб створити супер-сітки. |


## XYZ-GridPlot

![XYZ-GridPlot](XyzGridPlot/XyzGridPlot.png)

(ComfyUI workflow je uključen)

Генерише XYZ-Gridplot од листе слика.
Прима листу слика (укључујући и batches) и прво их сплита у дугу листу (дакле `batch_size=1`).

**Облик мреже**
Одређује облик мреже према:
1. броју ознака редова
2. броју ознака колона
3. преосталим подсликама.
Можете користити `order=inside_out` да обрнете избор слика (корисно ако је `batch_size>1` и желите да означите batches).

**Поравнање**
* Ако ознака прелази у следећи ред, цео ос се сматра "вишеструком линијом" и поравнају се на врху са равномерним размаком.
* Ако су све ознаке бројеви или све завршавају бројевима (нпр. `strength: 1.`) цео ос се сматра "бројчаним" и поравнају се удесно.
* Сви остали текстови се сматрају "једноструком линијом" и поравнају се у средишту.
* Једноструке и бројчане ознаке за колоне се поравнају на дну, а за редове се поравнају вертикално у средишту.

**Величина фонта**
* Висина подручја ознака колона одређује се према `font_size` или `пола највеће висине спајања подслика у било којем реду` (што је веће).
* Ширина подручја ознака редова одређује се на основу најшире ширине спајања подслика (са минимумом од 256px).
* Текст се смањује док не уђе у простор (до `font_size_min=6`) и користи исту величину фонта за цео ос (ознаке редова или колона).
Ако је величина фонта већ на минимуму, исеца се било који преостали текст.

**Спајање подслика**
Формира подслике (обично из batches) у најквадратнију површину („спајање подслика“), осим ако `output_is_list=True`, у том случају користи само једну слику за сваку ћелију и креира листу целокупних мрежа слика.
Можете користити ову листу мрежа слика да повежете још један XyzGridPlot чвор и креирате супер-мреже.
Ако подслике се састоје од batch-ова различитих величина, попуњава недостајуће ћелије празним slikama.
Број слика по ћелијама (укључујући batch-оване слике) мора бити дељив са `rows * columns`.

### Улази

| Име | Тип | Опис |
| --- | --- | --- |
| `images` | `IMAGE` | Листа слика (укључујући и batches) |
| `row_labels` | `*` | Текст ознака редова на левој страни |
| `col_labels` | `*` | Текст ознака колона на врху |
| `gap` | `INT` | Размак између спајања подслика. Напомените да унутар самих подслика не користи размак. Ако желите размак између подслика повежите још један XyzGridPlot чвор. |
| `font_size` | `FLOAT` | Циљана величина фонта. Текст ће се смањити док не уђе у простор (до `font_size_min=6`). |
| `row_label_orientation` | `COMBO` | Оријентација текста ознака редова. Корисно ако желите да уштедите простор. |
| `order` | `BOOLEAN` | Одређује у ком редоследу треба да се обраде слике. Ово је важно само ако имате подслике. Корисно ако је `batch_size>1` и желите да нацртате batches. |
| `output_is_list` | `BOOLEAN` | Ово је важно само ако имате подслике или желите да креирате супер-мреже. |

### Излази

| Име | Тип | Опис |
| --- | --- | --- |
| `image` | `IMAGE 𝌠` | XYZ-GridPlot слика. Ако је `output_is_list=True` креира листу слика коју можете повезати са још једним XYZ-GridPlot чвором да креирате супер-мреже. |


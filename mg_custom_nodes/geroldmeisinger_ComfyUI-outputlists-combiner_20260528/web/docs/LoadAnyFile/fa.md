## بارگذاری هر فایل

![Load Any File](LoadAnyFile/LoadAnyFile.png)

(فایل کارگاه ComfyUI همراه داده شده است)

هر فایل متنی یا باینری را بارگذاری کرده و محتوای فایل را به صورت رشته یا رشته base64 ارائه می‌دهد. همچنین سعی می‌کند آن را به عنوان `IMAGE` بارگذاری کند. و همچنین سعی می‌کند هر فایل metadata را بارگذاری کند.

`filepath` از مسیرهای فایل انnotated ComfyUI مانند `[input]` `[output]` یا `[temp]` پشتیبانی می‌کند.
`filepath` همچنین از گسترش الگوهای glob مانند `subdir/**/*.png` پشتیبانی می‌کند.
به صورت داخلی از [glob.iglob](https://docs.python.org/3/library/glob.html#glob.iglob) پایتون استفاده می‌کند.

`metadata` از `exiftool` استفاده می‌کند، اگر نصب و در `PATH` موجود باشد، در غیر این صورت از `PIL.Image.info` به عنوان فریب‌کار استفاده می‌کند.

به دلایل امنیتی فقط دایرکتوری‌های زیر پشتیبانی می‌شوند: `[input] [output] [temp]`.
به دلایل عملکردی تعداد فایل‌ها محدود به: 1024.

### ورودی‌ها

| نام | نوع | توضیح |
| --- | --- | --- |
| `filepath` | `STRING` | دایرکتوری پایه به صورت پیش‌فرض دایرکتوری کاربر `[input]` است. از گسترش الگوی glob مانند `subdir/**/*.png` پشتیبانی می‌کند. از پسوند ` [input]` ` [output]` یا ` [temp]` (فضای ابتدایی را در نظر بگیرید!) برای مشخص کردن دایرکتوری کاربر ComfyUI متفاوت استفاده کنید. |

### خروجی‌ها

| نام | نوع | توضیح |
| --- | --- | --- |
| `content` | `STRING 𝌠` | محتوای فایل برای فایل‌های متنی، base64 برای فایل‌های باینری. |
| `image` | `IMAGE 𝌠` | تانسور دسته تصویر. |
| `mask` | `MASK 𝌠` | تانسور دسته ماسک. |
| `metadata` | `STRING 𝌠` | داده‌های Exif از ExifTool. نیازمند دستور `exiftool` در `PATH` است. |


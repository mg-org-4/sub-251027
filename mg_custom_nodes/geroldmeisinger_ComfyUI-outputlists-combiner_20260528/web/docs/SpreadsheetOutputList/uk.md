## Вихідний список таблиці

![Вихідний список таблиці](SpreadsheetOutputList/SpreadsheetOutputList.png)

(Включено ComfyUI workflow)

Створює кілька вихідних списків з таблиці (`.csv .tsv .ods .xlsx .xls`).
Ви можете використовувати вузол `Load any File` для завантаження файлу в кодуванні base64.
Внутрішньо використовує *pandas* [read_excel](https://pandas.pydata.org/pandas-docs/stable/reference/api/pandas.read_excel.html) та [read_csv](https://pandas.pydata.org/pandas-docs/stable/reference/api/pandas.read_csv.html) для завантаження файлів таблиць.
Усі списки використовують `is_output_list=True` (позначено символом `𝌠`) і будуть оброблені послідовно відповідними вузлами.

### Входи

| Назва | Тип | Опис |
| --- | --- | --- |
| `rows_and_cols` | `STRING` | Індекси та назви рядків і стовпців у таблиці. Зверніть увагу, що в таблицях рядки починаються з 1, стовпці починаються з A, тоді як вихідні списки є 0-основними (в `select-nth`). |
| `header_rows` | `INT` | Ігнорувати перші x рядків у списку. Використовується лише якщо ви вказали стовпець у `rows_and_cols`. |
| `header_cols` | `INT` | Ігнорувати перші x стовпців у списку. Використовується лише якщо ви вказали рядок у `rows_and_cols`. |
| `select_nth` | `INT` | Вибрати лише n-й запис (0-основний). Корисно в комбінації з шаблоном `PrimitiveInt+control_after_generate=increment`. |
| `string_or_base64` | `STRING` | Рядок CSV/TSV або файл таблиці в base64 (для `.ods .xlsx .xls`). Використовуйте вузол `Load Any File` для завантаження файлу як base64. |

### Вихідні дані

| Назва | Тип | Опис |
| --- | --- | --- |
| `list_a` | `STRING 𝌠` |  |
| `list_b` | `STRING 𝌠` |  |
| `list_c` | `STRING 𝌠` |  |
| `list_d` | `STRING 𝌠` |  |
| `count` | `INT` | Кількість елементів у найдовшому списку. |


## Загрузить любой файл

![Load Any File](LoadAnyFile/LoadAnyFile.png)

(ComfyUI workflow включен)

Загружает любой текстовый или двоичный файл и предоставляет содержимое файла в виде строки или строки base64. Кроме того, пытается загрузить его как `IMAGE`. Также пытается загрузить любые метаданные.

`filepath` поддерживает аннотированные пути к файлам ComfyUI `[input]` `[output]` или `[temp]`.
`filepath` также поддерживает расширение шаблонов glob `subdir/**/*.png`.
Внутри использует python [glob.iglob](https://docs.python.org/3/library/glob.html#glob.iglob).

`metadata` вызывает `exiftool`, если он установлен и доступен в `PATH`, в противном случае использует `PIL.Image.info` как запасной вариант.

По соображениям безопасности поддерживаются только следующие директории: `[input] [output] [temp]`.
По соображениям производительности количество файлов ограничено: 1024.

### Входы

| Имя | Тип | Описание |
| --- | --- | --- |
| `filepath` | `STRING` | Базовая директория по умолчанию `[input]` пользовательская директория. Поддерживает расширение шаблона glob `subdir/**/*.png`. Используйте суффикс ` [input]` ` [output]` или ` [temp]` (обратите внимание на ведущий пробел!) чтобы указать другую пользовательскую директорию ComfyUI. |

### Выходы

| Имя | Тип | Описание |
| --- | --- | --- |
| `content` | `STRING 𝌠` | Содержимое файла для текстовых файлов, base64 для двоичных файлов. |
| `image` | `IMAGE 𝌠` | Тензор пакета изображений. |
| `mask` | `MASK 𝌠` | Тензор пакета масок. |
| `metadata` | `STRING 𝌠` | Exif данные из ExifTool. Требует наличия команды `exiftool` в `PATH`. |


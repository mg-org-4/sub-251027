## Строка OutputList

![String OutputList](StringOutputList/StringOutputList.png)

(ComfyUI workflow прилагается)

Создает OutputList, разделяя строку в текстовом поле с помощью разделителя.
`value` и `index` используют `is_output_list=True` (обозначено символом `𝌠`) и будут обработаны последовательно соответствующими узлами.

### Входы

| Имя | Тип | Описание |
| --- | --- | --- |
| `separator` | `STRING` | Строка, используемая для разделения значений текстового поля. |
| `values` | `STRING` | Текст, который вы хотите разделить на список. Обратите внимание, что строка обрезается от завершающих символов новой строки перед разделением, и каждый элемент снова обрезается от пробельных символов. |

### Выходы

| Имя | Тип | Описание |
| --- | --- | --- |
| `value` | `* 𝌠` | Значения из списка. |
| `index` | `INT 𝌠` | Диапазон от 0 до количества. Можно использовать как индекс. |
| `count` | `INT` | Количество элементов в списке. |
| `inspect_combo` | `COMBO` | Фиктивный выход, который можно использовать для подключения к `COMBO` и предварительного заполнения его значениями. Подключение затем будет автоматически переподключено к выходу `value`. |


## Числов Изходен Списък

![Number OutputList](NumberOutputList/NumberOutputList.png)

(Включва ComfyUI workflow)

Създава OutputList с диапазон от числови стойности.
Вътрешно използва [numpy.linspace](https://numpy.org/doc/stable/reference/generated/numpy.linspace.html), защото работи по-надеждно с дробни стойности.
Ако искате да определите списъци с произволни стъпки, проверете JSON Изходния Списък и определете масив, например `[1, 42, 123]`.
`int`, `float`, `string` и `index` използват `is_output_list=True` (обозначени със символа `𝌠`) и ще бъдат обработени последователно от съответните node-и.

### Входове

| Име | Тип | Описание |
| --- | --- | --- |
| `start` | `FLOAT` | Начална стойност за генериране на диапазона. |
| `stop` | `FLOAT` | Крайна стойност. Ако `endpoint=include`, тогава това число се включва в списъка. |
| `num` | `INT` | Броят на елементите в списъка (не объркайте с `step`). |
| `endpoint` | `BOOLEAN` | Определя дали стойността `stop` да се включи или изключи в елементите. |

### Изходи

| Име | Тип | Описание |
| --- | --- | --- |
| `int` | `INT 𝌠` | Стойността, преобразувана в цяло число (закръглена надолу/подложена). |
| `float` | `FLOAT 𝌠` | Стойността като дробно число. |
| `string` | `STRING 𝌠` | Стойността като дробно число, преобразувана в низ. |
| `index` | `INT 𝌠` | Диапазон от 0..count, който може да се използва като индекс. |
| `count` | `INT` | Същото като `num`. |


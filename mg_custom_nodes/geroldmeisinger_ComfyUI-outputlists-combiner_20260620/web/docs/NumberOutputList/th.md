## Number OutputList

![Number OutputList](NumberOutputList/NumberOutputList.png)

(ComfyUI workflow รวมอยู่ในไฟล์)

สร้าง OutputList ด้วยช่วงค่าตัวเลข
ใช้ [numpy.linspace](https://numpy.org/doc/stable/reference/generated/numpy.linspace.html) เป็นภายใน เพราะมันทำงานได้เสถียรกว่ากับค่าที่เป็นทศนิยม
หากคุณต้องการกำหนดรายการตัวเลขด้วยขั้นตอนที่กำหนดเอง ให้ลองดู JSON OutputList และกำหนดอาร์เรย์ เช่น `[1, 42, 123]`
`int`, `float`, `string` และ `index` ใช้ `is_output_list=True` (แสดงโดยสัญลักษณ์ `𝌠`) และจะถูกประมวลผลตามลำดับโดย node ที่สอดคล้องกัน

### ป้อนข้อมูล

| ชื่อ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| `start` | `FLOAT` | ค่าเริ่มต้นที่ใช้ในการสร้างช่วง |
| `stop` | `FLOAT` | ค่าสุดท้าย หาก `endpoint=include` แล้วค่าเหล่านี้จะถูกลิสต์ |
| `num` | `INT` | จำนวนรายการในลิสต์ (อย่าสับสนกับ `step`) |
| `endpoint` | `BOOLEAN` | กำหนดว่าจะรวมหรือไม่รวมค่า `stop` ในรายการ |

### ผลลัพธ์

| ชื่อ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| `int` | `INT 𝌠` | ค่าที่แปลงเป็น int (ปัดลง/ floored) |
| `float` | `FLOAT 𝌠` | ค่าเป็นทศนิยม |
| `string` | `STRING 𝌠` | ค่าเป็นทศนิยมที่แปลงเป็น string |
| `index` | `INT 𝌠` | ช่วง 0..count ซึ่งสามารถใช้เป็นดัชนีได้ |
| `count` | `INT` | เหมือนกับ `num` |


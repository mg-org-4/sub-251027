## โหลดไฟล์ใด ๆ

![Load Any File](LoadAnyFile/LoadAnyFile.png)

(รวม workflow ของ ComfyUI ไว้ด้วย)

โหลดไฟล์ข้อความหรือไบนารีใด ๆ และให้เนื้อหาของไฟล์เป็น string หรือ string base64 ยังพยายามโหลดเป็น `IMAGE` ด้วย และยังพยายามโหลดเมตาดาต้าใด ๆ ด้วย

`filepath` รองรับ annotated filepaths ของ ComfyUI `[input]` `[output]` หรือ `[temp]`
`filepath` ยังรองรับการขยายแบบ glob-pattern `subdir/**/*.png`
ใช้ python's [glob.iglob](https://docs.python.org/3/library/glob.html#glob.iglob) เป็นภายใน

`metadata` เรียก `exiftool` หากมันติดตั้งและมีอยู่ใน `PATH` ไม่เช่นนั้นจะใช้ `PIL.Image.info` เป็น fallback

เนื่องจากเหตุผลด้านความปลอดภัย จะรองรับได้เฉพาะไดเรกทอรีเหล่านี้เท่านั้น: `[input] [output] [temp]`
เนื่องจากเหตุผลด้านประสิทธิภาพ จำนวนไฟล์จะจำกัดไว้ที่: 1024

### ป้อนข้อมูล

| ชื่อ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| `filepath` | `STRING` | ไดเรกทอรีพื้นฐานจะเป็น `[input]` ไดเรกทอรีของผู้ใช้ รองรับการขยายแบบ glob-pattern `subdir/**/*.png` ใช้คำเสริม ` [input]` ` [output]` หรือ ` [temp]` (ระวังช่องว่างนำหน้า!) เพื่อกำหนดไดเรกทอรีของผู้ใช้ ComfyUI ที่แตกต่างกัน |

### ผลลัพธ์

| ชื่อ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| `content` | `STRING 𝌠` | เนื้อหาของไฟล์สำหรับไฟล์ข้อความ  base64 สำหรับไฟล์ไบนารี |
| `image` | `IMAGE 𝌠` | ภาพ batch tensor |
| `mask` | `MASK 𝌠` | ภาพ batch tensor |
| `metadata` | `STRING 𝌠` | ข้อมูล Exif จาก ExifTool ต้องมีคำสั่ง `exiftool` พร้อมใช้งานใน `PATH` |


## JSON OutputList

![JSON OutputList](JSONOutputList/JSONOutputList.png)

(ComfyUI workflow รวมอยู่ด้วย)

สร้าง OutputList โดยการดึงอาร์เรย์หรือดิกชันนารีจากวัตถุ JSON
ใช้ไวยากรณ์ JSONPath เพื่อดึงค่า ดู [JSONPath บนวิกิพีเดีย](https://en.wikipedia.org/wiki/JSONPath)
ค่าที่ตรงกันทั้งหมดจะถูกทำให้เป็นรายการยาวหนึ่งรายการ
คุณยังสามารถใช้ node นี้เพื่อสร้างวัตถุจากสตริงลิททิลเช่น `[1, 2, 3]`
`key`, `value`, `int` และ `float` ใช้ `is_output_list=True` (แสดงโดยสัญลักษณ์ `𝌠`) และจะถูกประมวลผลตามลำดับโดย node ที่สอดคล้องกัน

### ป้อนข้อมูล

| ชื่อ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| `jsonpath` | `STRING` | JSONPath ที่ใช้เพื่อดึงค่า |
| `json` | `STRING` | สตริง JSON ซึ่งจะแปลงเป็นวัตถุ |
| `obj` | `*` | (ไม่จำเป็น) วัตถุของประเภทใดก็ได้ ซึ่งจะแทนที่สตริง JSON |

### ผลลัพธ์

| ชื่อ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| `key` | `STRING 𝌠` | คีย์สำหรับดิกชันนารีหรือดัชนีสำหรับอาร์เรย์ (เป็นสตริง) ทางเทคนิคแล้วมันคือดัชนีสากลของรายการที่ทำให้เป็นเส้นตรงสำหรับทุกอย่างที่ไม่ใช่คีย์ |
| `value` | `STRING 𝌠` | ค่าเป็นสตริง |
| `int` | `INT 𝌠` | ค่าเป็น int (หากไม่สามารถแยกตัวเลขได้ จะใช้ค่าเริ่มต้นเป็น 0) |
| `float` | `FLOAT 𝌠` | ค่าเป็น float (หากไม่สามารถแยกตัวเลขได้ จะใช้ค่าเริ่มต้นเป็น 0) |
| `count` | `INT` | จำนวนรายการทั้งหมดในรายการที่ทำให้เป็นเส้นตรง |
| `debug` | `STRING` | ผลลัพธ์ดีบักของวัตถุที่ตรงกันทั้งหมดเป็นสตริง JSON ที่จัดรูปแบบแล้ว |


## Дискриминатор рабочих процессов

![Дискриминатор рабочих процессов](WorkflowDiscriminator/WorkflowDiscriminator.png)

(Включена рабочая流程 ComfyUI)

Сравнивает рабочие процессы и дискриминирует их для извлечения различных значений в виде отдельных списков вывода.
Вы можете использовать этот узел для восстановления того, как каждый отдельный изображение был создан из списка изображений с одинаковым рабочим процессом.
Обратите внимание, что метаданные рабочего процесса не содержатся в `IMAGE` ComfyUI, и вам нужно загрузить изображения с помощью специализированных загрузчиков изображений + метаданных и подключить метаданные к этому узлу.
Пользовательские узлы с загрузчиками метаданных включают:
* `Load Any File.metadata` -> `JSON OutputList(jsonpath=$.["PNG:Prompt"]).value`
* [Crystool](https://github.com/crystian/ComfyUI-Crystools) `🪛 Load image with metadata.Metadata RAW` -> `🪛 Metadata extractor.prompt`
* [Simple_Readable_Metadata](https://github.com/ShammiG/ComfyUI-Simple_Readable_Metadata-SG) `Simple Readable Metadata-SG.metadata_raw`

### Входы

| Имя | Тип | Описание |
| --- | --- | --- |
| `objs_0` | `*` | (необязательно) Один объект (или список объектов), обычно рабочего процесса. `objs_0` и `more_objs` будут объединены вместе и существуют для удобства, если вы хотите сравнить только два объекта. |
| `more_objs` | `*` | (необязательно) Еще один объект (или список объектов), обычно рабочего процесса. `objs_0` и `more_objs` будут объединены вместе и существуют для удобства, если вы хотите сравнить только два объекта. |
| `ignore_jsonpaths` | `STRING` | (необязательно) Список JSONPaths, которые нужно игнорировать, если вы хотите объединить несколько дискриминаторов. |

### Выходы

| Имя | Тип | Описание |
| --- | --- | --- |
| `list_a` | `* 𝌠` |  |
| `list_b` | `* 𝌠` |  |
| `list_c` | `* 𝌠` |  |
| `list_d` | `* 𝌠` |  |
| `jsonpaths` | `STRING 𝌠` |  |


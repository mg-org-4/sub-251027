## لیست خروجی رشته

![String OutputList](StringOutputList/StringOutputList.png)

(فایل کارگاه ComfyUI همراه داده شده است)

یک لیست خروجی را با تقسیم رشته در فیلد متنی با یک جداکننده ایجاد می‌کند.
`value` و `index` از `is_output_list=True` (که با نماد `𝌠` نشان داده شده است) استفاده می‌کنند و به صورت متوالی توسط نودهای مربوطه پردازش می‌شوند.

### ورودی‌ها

| نام | نوع | توضیح |
| --- | --- | --- |
| `separator` | `STRING` | رشته‌ای که برای تقسیم مقادیر فیلد متنی استفاده می‌شود. |
| `values` | `STRING` | متنی که می‌خواهید به یک لیست تقسیم شود. توجه داشته باشید که رشته قبل از تقسیم از خطوط جدید پایانی خالی می‌شود و هر آیتم دوباره از فضاهای خالی پاک می‌شود. |

### خروجی‌ها

| نام | نوع | توضیح |
| --- | --- | --- |
| `value` | `* 𝌠` | مقادیر از لیست. |
| `index` | `INT 𝌠` | محدوده 0..count. می‌توانید از این مقدار به عنوان یک شاخص استفاده کنید. |
| `count` | `INT` | تعداد آیتم‌های لیست. |
| `inspect_combo` | `COMBO` | خروجی ساختگی که می‌توانید برای اتصال به یک `COMBO` و پر کردن آن با مقادیر آن استفاده کنید. اتصال بعداً به صورت خودکار به خروجی `value` متصل می‌شود. |


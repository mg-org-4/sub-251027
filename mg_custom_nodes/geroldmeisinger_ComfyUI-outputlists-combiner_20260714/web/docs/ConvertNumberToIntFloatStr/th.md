<!-- This file was auto-translated with a local LLM and last updated on 2025-12-28. -->
## แปลงเป็นจำนวนเต็ม จำนวนทศนิยม ข้อความ

![Convert To Int Float Str](ConvertNumberToIntFloatStr/ConvertNumberToIntFloatStr.png)

(ComfyUI workflow included)

แปลงสิ่งใดสิ่งหนึ่งที่คล้ายกับจำนวนเป็น `INT` `FLOAT` `STRING`
ใช้ `nums_from_string.get_nums` ภายในซึ่งยอมรับจำนวนได้อย่างยืดหยุ่น ทั้งจำนวนเต็ม จำนวนทศนิยม จำนวนเต็มหรือทศนิยมในรูปแบบข้อความ ข้อความที่มีหลายจำนวนพร้อมเครื่องหมายพิเศษสำหรับพันร้อย
ใช้ข้อความ `123;234;345` เพื่อสร้างรายการของจำนวนอย่างรวดเร็ว อย่าใช้เครื่องหมายจุดทศนิยมเป็นตัวแยก เพราะอาจถูกตีความว่าเป็นเครื่องหมายพันร้อย
`int`, `float` และ `string` ใช้ `is_output_list=True` (แสดงด้วยสัญลักษณ์ `𝌠`) และจะถูกประมวลผลตามลำดับโดยโหนดที่เกี่ยวข้อง

### ตัวแปรนำเข้า

| ชื่อ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| `any` | `*` | สิ่งใดก็ตามที่สามารถแปลงเป็นข้อความได้อย่างมีความหมายและมีจำนวนที่สามารถอ่านได้ภายใน |

### ตัวแปรส่งออก

| ชื่อ | ประเภท | คำอธิบาย |
| --- | --- | --- |
| `int` | `INT 𝌠` | จำนวนทั้งหมดที่พบในข้อความ โดยตัดทศนิยมออก |
| `float` | `FLOAT 𝌠` | จำนวนทั้งหมดที่พบในข้อความในรูปแบบทศนิยม |
| `string` | `STRING 𝌠` | จำนวนทั้งหมดที่พบในข้อความที่ถูกแปลงเป็นข้อความ |
| `count` | `INT` | จำนวนของจำนวนที่พบในค่าที่กำหนด |


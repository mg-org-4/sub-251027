## مخرجات قائمة الأرقام

![مخرجات قائمة الأرقام](NumberOutputList/NumberOutputList.png)

(مدمج في ComfyUI workflow)

يُنشئ قائمة مخرجات بטווח من القيم الرقمية.
يستخدم [numpy.linspace](https://numpy.org/doc/stable/reference/generated/numpy.linspace.html) داخليًا، لأنه يعمل بشكل أكثر موثوقية مع القيم العشرية.
إذا كنت ترغب في تحديد قوائم الأرقام بخطوات عشوائية، فراجع مخرجات قائمة JSON وحدد مصفوفة، مثل `[1, 42, 123]`.
تستخدم `int` و `float` و `string` و `index` `is_output_list=True` (يُشير إليها برمز `𝌠`) وسيتم معالجتها تسلسليًا بواسطة العقد المقابلة.

### المدخلات

| الاسم | النوع | الوصف |
| --- | --- | --- |
| `start` | `FLOAT` | قيمة البداية لإنشاء النطاق منها. |
| `stop` | `FLOAT` | قيمة النهاية. إذا كانت `endpoint=include`، فسيتم تضمين هذا الرقم في القائمة. |
| `num` | `INT` | عدد العناصر في القائمة (لا تخلط بينها وبين `step`). |
| `endpoint` | `BOOLEAN` | يحدد ما إذا كانت قيمة `stop` يجب تضمينها أو استبعادها من العناصر. |

### المخرجات

| الاسم | النوع | الوصف |
| --- | --- | --- |
| `int` | `INT 𝌠` | القيمة المحوّلة إلى عدد صحيح (مُدور لأسفل/مُفلّت). |
| `float` | `FLOAT 𝌠` | القيمة كعدد عشري. |
| `string` | `STRING 𝌠` | القيمة كعدد عشري محوّلة إلى سلسلة. |
| `index` | `INT 𝌠` | نطاق 0..count الذي يمكن استخدامه كفهرس. |
| `count` | `INT` | نفس القيمة كما في `num`. |


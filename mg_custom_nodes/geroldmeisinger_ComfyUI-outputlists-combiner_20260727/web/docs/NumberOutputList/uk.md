## Number OutputList

![Number OutputList](NumberOutputList/NumberOutputList.png)

(ComfyUI workflow включено)

Створює OutputList із діапазоном числових значень.
Використовує [numpy.linspace](https://numpy.org/doc/stable/reference/generated/numpy.linspace.html) внутрішньо, оскільки він працює надійніше з числовими значеннями з плаваючою комою.
Якщо ви хочете визначити списки чисел з довільними кроками, перевірте JSON OutputList і визначте масив, наприклад `[1, 42, 123]`.
`int`, `float`, `string` та `index` використовують `is_output_list=True` (позначено символом `𝌠`) і будуть оброблені послідовно відповідними вузлами.

### Входи

| Ім'я | Тип | Опис |
| --- | --- | --- |
| `start` | `FLOAT` | Початкове значення для генерації діапазону. |
| `stop` | `FLOAT` | Кінцеве значення. Якщо `endpoint=include`, то це число включається до списку. |
| `num` | `INT` | Кількість елементів у списку (не плутати з `step`). |
| `endpoint` | `BOOLEAN` | Вирішує, чи слід включати або виключати значення `stop` у елементи. |

### Виходи

| Ім'я | Тип | Опис |
| --- | --- | --- |
| `int` | `INT 𝌠` | Значення, перетворене на int (округлене вниз/підлога). |
| `float` | `FLOAT 𝌠` | Значення як float. |
| `string` | `STRING 𝌠` | Значення як float, перетворене на рядок. |
| `index` | `INT 𝌠` | Діапазон 0..count, який можна використовувати як індекс. |
| `count` | `INT` | Те ж саме, що і `num`. |


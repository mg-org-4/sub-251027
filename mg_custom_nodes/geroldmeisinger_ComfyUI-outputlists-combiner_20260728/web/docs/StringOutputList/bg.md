## Изходен Списък С Низ

![String OutputList](StringOutputList/StringOutputList.png)

(Включва ComfyUI workflow)

Създава OutputList чрез разделяне на низа в текстовото поле с разделител.
`value` и `index` използват `is_output_list=True` (обозначени със символа `𝌠`) и ще бъдат обработени последователно от съответните node-и.

### Входове

| Име | Тип | Описание |
| --- | --- | --- |
| `separator` | `STRING` | Низът, използван за разделяне на стойностите в текстовото поле. |
| `values` | `STRING` | Текстът, който искате да разделите в списък. Имайте предвид, че низът се изчиства от завършващи нови редове преди разделяне, и всеки елемент се изчиства от интервали. |

### Изходи

| Име | Тип | Описание |
| --- | --- | --- |
| `value` | `* 𝌠` | Стойностите от списъка. |
| `index` | `INT 𝌠` | Диапазон от 0..count. Можете да използвате това като индекс. |
| `count` | `INT` | Броят на елементите в списъка. |
| `inspect_combo` | `COMBO` | Фалшив изход, който можете да използвате за свързване към `COMBO` и предварително попълване с неговите стойности. Свързването ще бъде автоматично пренасочено към изхода `value`. |


## Таблица OutputList

![Spreadsheet OutputList](SpreadsheetOutputList/SpreadsheetOutputList.png)

(ComfyUI workflow включён)

Создаёт несколько OutputList из таблицы (`.csv .tsv .ods .xlsx .xls`).
Вы можете использовать ноду `Load any File` для загрузки файла в кодировке base64.
Внутри используется *pandas* [read_excel](https://pandas.pydata.org/pandas-docs/stable/reference/api/pandas.read_excel.html) и [read_csv](https://pandas.pydata.org/pandas-docs/stable/reference/api/pandas.read_csv.html) для загрузки таблиц.
Все списки используют `is_output_list=True` (обозначено символом `𝌠`) и будут обрабатываться последовательно соответствующими нодами.

### Входы

| Имя | Тип | Описание |
| --- | --- | --- |
| `rows_and_cols` | `STRING` | Индексы и названия строк и столбцов в таблице. Обратите внимание, что в таблицах строки начинаются с 1, столбцы с A, тогда как OutputList используют 0-основание (в `select-nth`). |
| `header_rows` | `INT` | Игнорировать первые x строк в списке. Используется только если вы указали столбец в `rows_and_cols`. |
| `header_cols` | `INT` | Игнорировать первые x столбцов в списке. Используется только если вы указали строку в `rows_and_cols`. |
| `select_nth` | `INT` | Выбрать только nth элемент (0-основание). Полезно в комбинации с шаблоном `PrimitiveInt+control_after_generate=increment`. |
| `string_or_base64` | `STRING` | Строка CSV/TSV или файл таблицы в base64 (для `.ods .xlsx .xls`). Используйте ноду `Load Any File` для загрузки файла в base64. |

### Выходы

| Имя | Тип | Описание |
| --- | --- | --- |
| `list_a` | `STRING 𝌠` |  |
| `list_b` | `STRING 𝌠` |  |
| `list_c` | `STRING 𝌠` |  |
| `list_d` | `STRING 𝌠` |  |
| `count` | `INT` | Количество элементов в самом длинном списке. |


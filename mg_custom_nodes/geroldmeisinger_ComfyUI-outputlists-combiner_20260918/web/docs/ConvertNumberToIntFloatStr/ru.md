## Преобразовать в INT FLOAT STR

![Convert To Int Float Str](ConvertNumberToIntFloatStr/ConvertNumberToIntFloatStr.png)

(Включена ComfyUI workflow)

Преобразует всё, что похоже на число, в `INT` `FLOAT` `STRING`.
Внутри использует `nums_from_string.get_nums`, который очень терпим к принимаемым числам. Любые числа от целых чисел, вещественных чисел, целых или вещественных чисел в виде строк, строк, содержащих несколько чисел с разделителями тысяч.
Используйте строку `123;234;345` для быстрого создания списка чисел. Не используйте запятые в качестве разделителей, так как они могут быть интерпретированы как разделители тысяч.
`int`, `float` и `string` используют `is_output_list=True` (обозначено символом `𝌠`) и будут обрабатываться последовательно соответствующими узлами.

### Входы

| Имя | Тип | Описание |
| --- | --- | --- |
| `any` | `*` | Всё, что может быть успешно преобразовано в строку с разбором чисел внутри |

### Выходы

| Имя | Тип | Описание |
| --- | --- | --- |
| `int` | `INT 𝌠` | Все числа, найденные в строке, с отсечёнными десятичными знаками. |
| `float` | `FLOAT 𝌠` | Все числа, найденные в строке, в виде вещественных чисел. |
| `string` | `STRING 𝌠` | Все числа, найденные в строке, в виде вещественных чисел, преобразованных в строку. |
| `count` | `INT` | Количество чисел, найденных в значении. |


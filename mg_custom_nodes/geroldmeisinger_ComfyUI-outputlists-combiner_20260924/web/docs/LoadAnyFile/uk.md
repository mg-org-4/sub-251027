## Завантажити будь-який файл

![Load Any File](LoadAnyFile/LoadAnyFile.png)

(Включено ComfyUI workflow)

Завантажує будь-який текстовий або бінарний файл і надає вміст файлу у вигляді рядка або base64 рядка. Також намагається завантажити його як `IMAGE`. Також намагається завантажити будь-які метадані.

`filepath` підтримує анотовані шляхи файлів ComfyUI `[input]` `[output]` або `[temp]`.
`filepath` також підтримує розширення шаблонів glob `subdir/**/*.png`.
Внутрішньо використовує python [glob.iglob](https://docs.python.org/3/library/glob.html#glob.iglob).

`metadata` викликає `exiftool`, якщо він встановлений і доступний у `PATH`, інакше використовує `PIL.Image.info` як запасний варіант.

З міркувань безпеки підтримуються лише наступні каталоги: `[input] [output] [temp]`.
З міркувань продуктивності кількість файлів обмежена: 1024.

### Вхідні дані

| Назва | Тип | Опис |
| --- | --- | --- |
| `filepath` | `STRING` | Базова директорія за замовчуванням `[input]` користувацька директорія. Підтримує розширення шаблонів glob `subdir/**/*.png`. Використовуйте суфікс ` [input]` ` [output]` або ` [temp]` (зверніть увагу на початковий пробіл!) щоб вказати іншу користувацьку директорію ComfyUI. |

### Вихідні дані

| Назва | Тип | Опис |
| --- | --- | --- |
| `content` | `STRING 𝌠` | Вміст файлу для текстових файлів, base64 для бінарних файлів. |
| `image` | `IMAGE 𝌠` | Тензор пакета зображень. |
| `mask` | `MASK 𝌠` | Тензор пакета масок. |
| `metadata` | `STRING 𝌠` | Дані Exif з ExifTool. Потрібна команда `exiftool` у `PATH`. |


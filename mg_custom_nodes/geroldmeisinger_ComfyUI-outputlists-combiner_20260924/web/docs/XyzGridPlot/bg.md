## XYZ-GridPlot

![XYZ-GridPlot](XyzGridPlot/XyzGridPlot.png)

(Включен е ComfyUI workflow)

Генерира XYZ-Gridplot от списък с изображения.
Той взима списък с изображения (включително batches) и ги изравнява в дълъг списък първо (така че `batch_size=1`).

**Форма на мрежата**
Определя формата на мрежата чрез:
1. броя на етикетите на редовете
2. броя на етикетите на колоните
3. останалите под-изображения.
Можете да използвате `order=inside_out` за да обърнете избора на изображения (полезно ако `batch_size>1` и искате да етикетирате пакетите).

**Подравняване**
* Ако етикетът се премества на следващия ред, цялата ос се счита за "многоредова" и се подравнява отгоре с разпределен интервал.
* Ако всички етикети са числа или всички завършват с числа (напр. `strength: 1.`) цялата ос се счита за "числова" и се подравнява вдясно.
* Всички други текстове се считат за "едноредови" и се подравняват в средата.
* Подравнява едноредови и числови етикети за колони в долната част, а за редове ги подравнява вертикално в средата.

**Размер на шрифта**
* Височината на областта с етикети на колоните се определя от `font_size` или `половината от най-голямата височина на пакетиране на под-изображения в някой ред` (в зависимост от това кое е по-голямо).
* Ширината на областта с етикети на редовете се определя от най-широката ширина на пакетирането на под-изображения (с минимум 256px).
* Текстът се намалява, докато се побере (до `font_size_min=6`) и използва същия размер на шрифта за цялата ос (етикети на редове или колони).
Ако размерът на шрифта вече е минимален, отрязва всеки оставащ текст.

**Пакетиране на под-изображения**
Формира под-изображенията (обикновено от пакети) в най-квадратната област („пакетиране на под-изображения“), освен ако `output_is_list=True`, в този случай използва само едно изображение за всяка клетка и създава списък с цели мрежи на изображения вместо това.
Можете да използвате този списък с мрежи на изображения, за да свържете друг XYZ-GridPlot node и да създадете супер-мрежи.
Ако под-изображенията са от пакети с различни размери, попълва липсващите клетки с празни изображения.
Броят на изображенията на клетки (включително пакетирани изображения) трябва да бъде кратен на `rows * columns`.

### Входове

| Име | Тип | Описание |
| --- | --- | --- |
| `images` | `IMAGE` | Списък с изображения (включително batches) |
| `row_labels` | `*` | Текстове на етикетите на редовете вляво |
| `col_labels` | `*` | Текстове на етикетите на колоните отгоре |
| `gap` | `INT` | Разстояние между пакетирането на под-изображенията. Имайте предвид, че вътре в под-изображенията не се използва разстояние. Ако искате разстояние между под-изображенията, свържете друг XYZ-GridPlot node. |
| `font_size` | `FLOAT` | Целеви размер на шрифта. Текстът ще бъде намален, докато се побере (до `font_size_min=6`). |
| `row_label_orientation` | `COMBO` | Ориентация на текста на етикетите на редовете. Полезно, ако искате да спестите място. |
| `order` | `BOOLEAN` | Определя в какъв ред да се обработват изображенията. Това е важно само ако имате под-изображения. Полезно, ако `batch_size>1` и искате да визуализирате пакетите. |
| `output_is_list` | `BOOLEAN` | Това е важно само ако имате под-изображения или искате да създадете супер-мрежи. |

### Изходи

| Име | Тип | Описание |
| --- | --- | --- |
| `image` | `IMAGE 𝌠` | Изображението на XYZ-GridPlot. Ако `output_is_list=True` създава списък с изображения, които можете да свържете към друг XYZ-GridPlot node, за да създадете супер-мрежи. |


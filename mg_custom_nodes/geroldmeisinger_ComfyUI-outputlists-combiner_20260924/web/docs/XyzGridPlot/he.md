## רשת תרשים XYZ

![XYZ-GridPlot](XyzGridPlot/XyzGridPlot.png)

(הזרימה ב-ComfyUI נכללת)

יוצר תרשים רשת XYZ מרשימה של תמונות.
זה לוקח רשימה של תמונות (כולל קבוצות) ומחזיר אותן לרשימה ארוכה ראשונה (לכן `batch_size=1`).

**צורת רשת**
מגדיר את צורת הרשת על ידי:
1. מספר תווים של שורות
2. מספר תווים של עמודות
3. תת-התמונות הנותרות.
אתה יכול להשתמש ב-`order=inside_out` כדי להפוך את בחירת התמונות (שימושי אם `batch_size>1` וברצונך לתווית את הקבוצות).

**יישור**
* אם תווית נפתחת לשורה הבאה כל المحور נחשב "רב-שורה" ויישר אותם למעלה עם רווח מותאם.
* אם כל התווית היא מספר או כל אחת מסתיימת במספר (למשל `strength: 1.`) כל المحور נחשב "נומרי" ויישר אותם ימינה.
* כל טקסט אחר נחשב "חד-שורה" ויישר אותם במרכז.
* מיישר תווית חד-שורה ונומריית של עמודות למטה, ועבור שורות מיישר אותם אנכית במרכז.

**גודל גופן**
* הגובה של אזור התווית של העמודה נקבע על ידי `font_size` או `מחצית גובה האחסון של תת-התמונות הגדול ביותר בכל שורה` (המכיל את הגדול).
* הרוחב של אזור התווית של השורה נקבע על ידי הרוחב הגדול ביותר של אכסון תת-התמונות (עם מינימום של 256px).
* הטקסט נוקט עד שהוא מתאים (עד `font_size_min=6`) ומשתמש באותו גודל גופן עבור כל المحور (תווית שורה או תווית עמודה).
אם גודל הגופן כבר במקסימום, חותך כל טקסט שנותר.

**אחסון תת-התמונות**
מחזיר את תת-התמונות (عادة מהקבוצות) לאזור הריבועי (ה-"אחסון תת-התמונות"), אלא אם כן `output_is_list=True`, כאשר זה משתמש רק בתמונה אחת לכל תא ווצר רשימה של רשתות תמונות שלמה במקום.
אתה יכול להשתמש ברשימה של רשתות התמונות כדי לקשר לצומת XyzGridPlot אחר כדי ליצור רשתות ענקיות.
אם תת-התמונות מכילות קבוצות בגדלים שונים, מילא את התאים החסרים בתמונות ריקות.
מספר התמונות לכל תאים (כולל תמונות קבוצתיות) חייב להיות مضاعף של `rows * columns`.

### קלטים

| שם | סוג | תיאור |
| --- | --- | --- |
| `images` | `IMAGE` | רשימה של תמונות (כולל קבוצות) |
| `row_labels` | `*` | טקסטים של תווית השורות בצד שמאל |
| `col_labels` | `*` | טקסטים של תווית העמודות בצד העליון |
| `gap` | `INT` | רווח בין אכסון תת-התמונות. שים לב שפנימה בתמונות עצמו אין רווח. אם אתה רוצה רווח בין תת-התמונות קשור לצומת XyzGridPlot אחר. |
| `font_size` | `FLOAT` | גודל גופן יעד. הטקסט יקטן עד שהוא מתאים (עד `font_size_min=6`). |
| `row_label_orientation` | `COMBO` | כיוון טקסט של תווית השורות. שימושי אם אתה רוצה לחסוך מקום. |
| `order` | `BOOLEAN` | מגדיר באיזה סדר יש לעבד את התמונות. זה רלוונטי רק אם יש לך תת-תמונות. שימושי אם `batch_size>1` וברצונך לצייר את הקבוצות. |
| `output_is_list` | `BOOLEAN` | זה רלוונטי רק אם יש לך תת-תמונות או שאתה רוצה ליצור רשתות ענקיות. |

### פלטים

| שם | סוג | תיאור |
| --- | --- | --- |
| `image` | `IMAGE 𝌠` | התמונה של תרשים רשת XYZ. אם `output_is_list=True` יוצר רשימה של תמונות שתוכלו לקשר לצומת XYZ-GridPlot אחר כדי ליצור רשתות ענקיות. |


[project]
name = "rocm-ninodes"
description = "ROCM Optimized Nodes for ComfyUI - High-performance VAE decode and sampling nodes specifically tuned for AMD GPUs with ROCm support, particularly targeting gfx1151 architecture"
version = "2.3.4"
license = {file = "LICENSE"}
dependencies = [
    "numpy>=1.20.0",
    "Pillow>=8.0.0",
    "gguf>=0.7.0",
    "safetensors>=0.4.0",
]

[project.urls]
Repository = "https://github.com/iGavroche/rocm-ninodes"

[tool.comfy]
PublisherId = "igavroche"
DisplayName = "ROCm Ninodes"
Icon = "https://raw.githubusercontent.com/iGavroche/rocm-ninodes/refs/heads/main/icon.jpg"
includes = ["nodes.py", "web/", "comfyui_manager.json"]
requires-comfyui = ">=0.0.1"

[tool.comfyui-manager]
name = "rocm-ninodes"
display_name = "ROCm Ninodes"
description = "ROCm-optimized nodes for ComfyUI: VAE decode, sampling, SamplerCustomAdvanced drop-in, loaders, LTX2 prompt generation, and monitoring — tuned for AMD GPUs (gfx1151) with ROCm."
version = "2.3.4"
author = "iGavroche"
author_email = "nino2k@proton.me"
repository = "https://github.com/iGavroche/rocm-ninodes"
license = "MIT"
nodes = [
    "ROCmCheckpointLoader",
    "ROCmDiffusionLoader",
    "ROCmGGUFLoader",
    "ROCmVAEDecode",
    "ROCmVAEDecodeTiled",
    "ROCmVAEPerformanceMonitor",
    "ROCmKSampler",
    "ROCmKSamplerAdvanced",
    "ROCmSamplerCustomAdvanced",
    "ROCmSamplerCustomAdvancedBenchmark",
    "ROCmSamplerPerformanceMonitor",
    "ROCmFluxBenchmark",
    "ROCmMemoryOptimizer",
    "ROCmLoRALoader",
    "ROCmTextGenerateLTX2Prompt",
    "ROCmH3EasyCache"
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "torch>=2.10.0",
]

# RocM-Nino Requirements
# Core dependencies for ROCM optimized nodes

# PyTorch with ROCm support (install separately)
# torch>=2.0.0+rocm6.4

# Standard dependencies
numpy>=1.20.0
Pillow>=8.0.0
psutil>=5.8.0
gguf>=0.7.0
safetensors>=0.4.0

# Optional dependencies for enhanced performance
# triton>=2.0.0  # For AMD attention optimizations